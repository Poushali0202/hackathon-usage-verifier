/* Generated by tools/gen-evaluator-bundle.mjs — do not edit. */
/** Flat RestrictedPython-safe evaluator (target + fetch shim + engine + extract + drivers). */
export const RESTRICTED_BUNDLE: string = "# Generated by tools/build_restricted_bundle.py - do not edit. Flat, RestrictedPython-safe\n# evaluator: target.py + fetch shim + engine.py + extract.py + drivers.\nimport json\nimport re\nimport time\nfrom collections import namedtuple\n\nPRESET_JSON = \"{\\n  \\\"slug\\\": \\\"rocketride\\\",\\n  \\\"name\\\": \\\"RocketRide\\\",\\n  \\\"types\\\": [\\\"code\\\", \\\"platform\\\"],\\n  \\\"pipeline_scoring\\\": true,\\n  \\\"regex\\\": {\\n    \\\"sdk_call\\\": \\\"\\\\\\\\b(RocketRideClient|client\\\\\\\\.use|client\\\\\\\\.chat|client\\\\\\\\.send|client\\\\\\\\.connect)\\\",\\n    \\\"dependency\\\": \\\"rocketride|@rocketride/sdk\\\",\\n    \\\"inline_pipe\\\": \\\"client\\\\\\\\.use\\\\\\\\(\\\\\\\\s*\\\\\\\\{\\\\\\\\s*pipeline\\\\\\\\b\\\",\\n    \\\"runner\\\": \\\"client\\\\\\\\.use\\\\\\\\(\\\\\\\\s*\\\\\\\\{?\\\\\\\\s*(?:file)?path\\\\\\\\s*[=:]\\\\\\\\s*(?!['\\\\\\\"])\\\",\\n    \\\"http\\\": \\\"api\\\\\\\\.rocketride\\\\\\\\.ai|rocketride\\\\\\\\.ai/[\\\\\\\\w/-]*(pipeline|webhook|hook|run|invoke)\\\",\\n    \\\"hosted_env\\\": \\\"rocketride_\\\\\\\\w*(url|webhook|endpoint|hook)\\\",\\n    \\\"src_hint\\\": \\\"rocket|pipeline|integration|agent|engine|config|api|route|server|main|index|app|relay|deploy\\\",\\n    \\\"name_in_path\\\": \\\"rocketride\\\"\\n  },\\n  \\\"tokens\\\": {\\n    \\\"invoke\\\": [\\\"client.use\\\", \\\"client.send\\\", \\\"client.chat\\\", \\\"rocketride start\\\"],\\n    \\\"import_hints\\\": [\\\"from rocketride\\\", \\\"import rocketride\\\", \\\"@rocketride\\\"],\\n    \\\"hosted\\\": [\\\"rocketride_pipeline\\\", \\\"lib/rocketride\\\"],\\n    \\\"engine\\\": [\\\"ws://localhost:5565\\\", \\\"/v1/pipelines\\\"]\\n  },\\n  \\\"artifact_extensions\\\": [\\\".pipe\\\"],\\n  \\\"json_pipelines\\\": true,\\n  \\\"competitors\\\": [\\\"langchain\\\", \\\"crewai\\\"],\\n  \\\"scaffold_paths\\\": [\\\".claude/rules/rocketride.md\\\"],\\n  \\\"platform\\\": { \\\"domains\\\": [], \\\"files\\\": [], \\\"markers\\\": [] },\\n  \\\"weights\\\": {\\n    \\\"dependency\\\": 1.0,\\n    \\\"pipeline_called\\\": 2.0,\\n    \\\"agent_node\\\": 1.0,\\n    \\\"complexity_mid\\\": 0.5,\\n    \\\"complexity_high\\\": 1.0,\\n    \\\"tool_or_llm\\\": 0.5,\\n    \\\"hosted\\\": 1.0,\\n    \\\"sdk_callsites\\\": 0.5,\\n    \\\"file_spread\\\": 0.5,\\n    \\\"uncalled_penalty\\\": 1.0,\\n    \\\"predates_penalty\\\": 1.0\\n  },\\n  \\\"thresholds\\\": { \\\"significant\\\": 4.0, \\\"moderate\\\": 2.0, \\\"less\\\": 1.0 }\\n}\\n\"\n\n\"\"\"TargetConfig - the product being verified, as data.\n\nThe engine is purely target-agnostic: every check reads from a Target. Two construction\npaths:\n\n  • Target.from_preset(dict)  - trusted JSON (eval/targets/*.json) carrying explicit regex\n    sources. The bundled RocketRide preset reproduces the historical constants verbatim,\n    which is what guarantees fixture parity.\n  • Target.from_ui_config(name, dict) - an end user's saved target (free-text fields from\n    the Targets editor). Tokens are treated as LITERALS (regex-escaped); matching is\n    case-insensitive substring/alternation. No user input is ever compiled as raw regex.\n\nNothing about any specific product (Butterbase, Supabase, ...) lives in code - only in\nconfig. `pipeline_scoring` selects the artifact-graph scoring path (RocketRide pipelines);\ngeneric targets score on dependency / invocation depth / API usage / platform evidence.\n\"\"\"\nimport json\nimport re\nfrom dataclasses import dataclass, field\nGENERIC_WEIGHTS = {'dependency': 1.0, 'invocation': 1.5, 'invocation_deep': 1.0, 'api_usage': 1.5, 'hosted': 0.5, 'file_spread': 0.5, 'artifact': 1.0, 'platform_deploy': 1.5, 'predates_penalty': 1.0, 'uncalled_penalty': 1.0}\nGENERIC_THRESHOLDS = {'significant': 4.0, 'moderate': 2.0, 'less': 1.0}\nARCHITECTURE_TEMPLATES = {'sdk': [{'id': 'install', 'label': 'Package installed', 'signal': 'dependency', 'load_bearing': False}, {'id': 'wired', 'label': 'Configured / keys', 'signal': 'hosted', 'load_bearing': False}, {'id': 'client', 'label': 'Called in code', 'signal': 'invocation', 'load_bearing': True}, {'id': 'runtime', 'label': 'Runtime API', 'signal': 'api_usage', 'load_bearing': True}, {'id': 'hosted', 'label': 'Shipped / hosted', 'signal': 'platform_deploy', 'load_bearing': False}], 'data_platform': [{'id': 'install', 'label': 'Package installed', 'signal': 'dependency', 'load_bearing': False}, {'id': 'config', 'label': 'Platform config', 'signal': 'artifact', 'load_bearing': False}, {'id': 'client', 'label': 'Client / SDK', 'signal': 'invocation', 'load_bearing': True}, {'id': 'data', 'label': 'Data plane / API', 'signal': 'api_usage', 'load_bearing': True}, {'id': 'hosted', 'label': 'Hosted on platform', 'signal': 'platform_deploy', 'load_bearing': True}], 'api': [{'id': 'install', 'label': 'Package / client', 'signal': 'dependency', 'load_bearing': False}, {'id': 'auth', 'label': 'Auth / keys', 'signal': 'hosted', 'load_bearing': False}, {'id': 'client', 'label': 'Called in code', 'signal': 'invocation', 'load_bearing': False}, {'id': 'runtime', 'label': 'Runtime API', 'signal': 'api_usage', 'load_bearing': True}, {'id': 'hosted', 'label': 'Live endpoint', 'signal': 'platform_deploy', 'load_bearing': True}], 'deploy': [{'id': 'install', 'label': 'Package / project', 'signal': 'dependency', 'load_bearing': False}, {'id': 'config', 'label': 'Deploy config', 'signal': 'artifact', 'load_bearing': False}, {'id': 'account', 'label': 'Account / env', 'signal': 'hosted', 'load_bearing': False}, {'id': 'runtime', 'label': 'Preview / API', 'signal': 'api_usage', 'load_bearing': False}, {'id': 'hosted', 'label': 'Live on platform', 'signal': 'platform_deploy', 'load_bearing': True}]}\n\ndef infer_architecture_template(types, explicit=None):\n    if explicit and explicit in ARCHITECTURE_TEMPLATES:\n        return explicit\n    tset = {str(x).lower() for x in types or ['code']}\n    if 'platform' in tset and ('code' in tset or 'api' in tset):\n        return 'data_platform'\n    if 'platform' in tset:\n        return 'deploy'\n    if 'api' in tset and 'code' not in tset:\n        return 'api'\n    return 'sdk'\n\ndef hj_signal_on(signal, evidence):\n    sdk = evidence.get('sdk') or {}\n    pf = evidence.get('platform') or {}\n    if signal == 'dependency':\n        return bool(evidence.get('dependency'))\n    if signal == 'invocation':\n        return int(sdk.get('callsites') or 0) > 0\n    if signal == 'invocation_deep':\n        return int(sdk.get('callsites') or 0) >= 8\n    if signal == 'api_usage':\n        return bool(sdk.get('engine'))\n    if signal == 'hosted':\n        return bool(sdk.get('hosted'))\n    if signal == 'file_spread':\n        return int(sdk.get('file_spread') or 0) >= 2\n    if signal == 'artifact':\n        return bool(pf.get('files'))\n    if signal == 'platform_deploy':\n        return bool(pf.get('domains'))\n    return False\n\ndef architecture_view(target, evidence):\n    \"\"\"Map this target's architecture panes onto signals we already detected.\n\n    Panes are a VIEW of generic evidence, never a second scoring path. A streaming\n    SDK, a BaaS, and an HTTP API all use the same signals with different labels.\n    \"\"\"\n    custom = [p for p in target.architecture or [] if isinstance(p, dict) and p.get('id')]\n    if custom:\n        spec = custom[:5]\n    else:\n        slug = infer_architecture_template(target.types, target.architecture_template)\n        spec = ARCHITECTURE_TEMPLATES[slug]\n    out = []\n    for pane in spec[:5]:\n        signal = str(pane.get('signal') or '')\n        out.append({'id': str(pane.get('id') or signal or 'pane'), 'label': str(pane.get('label') or signal or 'Capability'), 'signal': signal, 'load_bearing': bool(pane.get('load_bearing')), 'state': 'target' if hj_signal_on(signal, evidence) else 'none'})\n    return out\n\ndef hj_name_derived_tokens(name):\n    \"\"\"Identifiers a display name implies so 'Hydra DB' matches db_hydradb in a .pipe.\"\"\"\n    slug = re.sub('[^a-z0-9]+', '-', (name or '').lower()).strip('-')\n    compact = re.sub('[^a-z0-9]', '', (name or '').lower())\n    out = []\n    if len(compact) >= 6:\n        out.append(compact)\n        out.append('db_' + compact)\n    if slug and '-' in slug and (len(slug) >= 6):\n        out.append(slug)\n        out.append(slug.replace('-', '_'))\n    return list(dict.fromkeys(out))\n\ndef hj_split_tokens(text):\n    \"\"\"Free-text field -> clean literal tokens. Splits on commas and pipes, strips\n    parenthetical hints and glob stars, drops empties and one-char noise.\"\"\"\n    out = []\n    for raw in re.split('[|,\\\\n]', text or ''):\n        tok = re.sub('\\\\([^)]*\\\\)', '', raw).strip().strip('*').strip()\n        if len(tok) >= 2:\n            out.append(tok)\n    return out\n\ndef hj_alt_regex(tokens):\n    if not tokens:\n        return None\n    return re.compile('|'.join((re.escape(t) for t in tokens)), re.I)\n\ndef hj_num_map(overrides, defaults):\n    \"\"\"Merge user-tuned rubric numbers over the defaults. Values arrive as strings from\n    the editor; anything unparseable or negative falls back to the default for that key,\n    and unknown keys are ignored (the engine only reads known signals).\"\"\"\n    out = dict(defaults)\n    for k, v in (overrides or {}).items():\n        if k in out:\n            try:\n                f = float(v)\n                if f >= 0:\n                    out[k] = f\n            except (TypeError, ValueError):\n                pass\n    return out\nTarget = namedtuple('Target', ['slug', 'name', 'types', 'pipeline_scoring', 'sdk_call_rx', 'dependency_rx', 'inline_pipe_rx', 'runner_rx', 'http_rx', 'hosted_env_rx', 'src_hint_rx', 'name_in_path_rx', 'invoke_tokens', 'import_hints', 'hosted_tokens', 'engine_tokens', 'artifact_extensions', 'json_pipelines', 'artifact_paths', 'platform_domains', 'platform_files', 'platform_markers', 'competitors', 'neutral', 'scaffold_paths', 'weights', 'thresholds', 'architecture_template', 'architecture'], defaults=['', '', ['code'], False, None, None, None, None, None, None, None, None, (), (), (), (), (), False, (), (), (), (), frozenset(), frozenset(), (), {}, {}, '', []])\n\ndef target_from_preset(cfg):\n    rx = cfg.get('regex', {})\n    tk = cfg.get('tokens', {})\n    pf = cfg.get('platform', {})\n    c = lambda k: re.compile(rx[k], re.I) if rx.get(k) else None\n    return Target(slug=cfg['slug'], name=cfg['name'], types=cfg.get('types', ['code']), pipeline_scoring=bool(cfg.get('pipeline_scoring')), sdk_call_rx=c('sdk_call'), dependency_rx=c('dependency'), inline_pipe_rx=c('inline_pipe'), runner_rx=c('runner'), http_rx=c('http'), hosted_env_rx=c('hosted_env'), src_hint_rx=c('src_hint'), name_in_path_rx=c('name_in_path'), invoke_tokens=tuple((t.lower() for t in tk.get('invoke', []))), import_hints=tuple((t.lower() for t in tk.get('import_hints', []))), hosted_tokens=tuple((t.lower() for t in tk.get('hosted', []))), engine_tokens=tuple((t.lower() for t in tk.get('engine', []))), artifact_extensions=tuple(cfg.get('artifact_extensions', [])), json_pipelines=bool(cfg.get('json_pipelines')), platform_domains=tuple((t.lower() for t in pf.get('domains', []))), platform_files=tuple((t.lower() for t in pf.get('files', []))), platform_markers=tuple((t.lower() for t in pf.get('markers', []))), competitors=frozenset((t.lower() for t in cfg.get('competitors', []))), scaffold_paths=tuple(cfg.get('scaffold_paths', [])), weights=dict(cfg.get('weights', {})), thresholds=dict(cfg.get('thresholds', {})), architecture_template=str(cfg.get('architecture_template') or ''), architecture=list(cfg.get('architecture') or []))\n\ndef target_from_ui_config(name, cfg):\n    \"\"\"Build from the Targets editor's free-text fields (all literals, never raw regex).\"\"\"\n    deps = hj_split_tokens(cfg.get('dependency_names'))\n    invoc = hj_split_tokens(cfg.get('invocation'))\n    hosted = hj_split_tokens(cfg.get('hosted_markers'))\n    cli = hj_split_tokens(cfg.get('cli_verbs'))\n    arts = hj_split_tokens(cfg.get('artifacts'))\n    derived = hj_name_derived_tokens(name)\n    deps = list(dict.fromkeys(deps + derived))\n    invoc = list(dict.fromkeys(invoc + derived))\n    domains = [t.lstrip('*.').lower() for t in hj_split_tokens(cfg.get('platform_domains'))]\n    pfiles = hj_split_tokens(cfg.get('platform_files'))\n    markers = hj_split_tokens(cfg.get('platform_markers'))\n    slug = re.sub('[^a-z0-9]+', '-', name.lower()).strip('-') or 'target'\n    hosts = [t.lower() for t in invoc + hosted if '.' in t and ' ' not in t]\n    return Target(slug=slug, name=name, types=list(cfg.get('types') or ['code']), pipeline_scoring=False, sdk_call_rx=hj_alt_regex(invoc + cli), dependency_rx=hj_alt_regex(deps), src_hint_rx=hj_alt_regex([name] + deps + ['api', 'client', 'server', 'main', 'index', 'app', 'config', 'lib']), name_in_path_rx=hj_alt_regex([name] + deps), invoke_tokens=tuple((t.lower() for t in invoc + cli)), import_hints=tuple((t.lower() for t in deps)), hosted_tokens=tuple((t.lower() for t in hosted + markers)), engine_tokens=tuple(dict.fromkeys(hosts + domains)), artifact_paths=tuple((t.lower() for t in arts + pfiles)), platform_domains=tuple(dict.fromkeys(domains)), platform_files=tuple((t.lower() for t in pfiles)), platform_markers=tuple((t.lower() for t in markers + hosted)), competitors=frozenset((t.lower() for t in hj_split_tokens(cfg.get('competitors')))), neutral=frozenset((t.lower() for t in hj_split_tokens(cfg.get('neutral')))), weights=hj_num_map(cfg.get('weights'), GENERIC_WEIGHTS), thresholds=hj_num_map(cfg.get('thresholds'), GENERIC_THRESHOLDS), architecture_template=str(cfg.get('architecture_template') or ''), architecture=list(cfg.get('architecture') or []))\n\n# Fetch shim for the tool_python evaluator (RestrictedPython-safe: no leading-underscore names,\n# no os/sys/subprocess, nothing on disk). Every byte of a repository comes from the GitHub\n# REST API / raw.githubusercontent.com; there is no git clone anywhere in this path.\n#\n# Two fetch modes, chosen at run time:\n#   direct  - urllib is importable in this engine's sandbox: fetch here with the judge's token.\n#   replay  - urllib is not allowed (staging today): the app fetches over the catalog\n#             tool_http_request node and hands the bodies in as CACHE; any URL the evaluator\n#             asks for that is not cached is reported back as `missing` and the app re-runs.\n#             Selection of what to fetch stays 100% in the evaluator - the app never decides.\nOTHER_PLATFORMS = [\"butterbase\", \"supabase\", \"xtrace\", \"photon\", \"langchain\",\n                   \"crewai\", \"firebase\", \"pinecone\", \"weaviate\"]\nUA = \"hackjudge-python-v1\"\nGITHUB_HOSTS = (\"api.github.com\", \"raw.githubusercontent.com\")\nRETRY_STATUSES = (429, 500, 502, 503)\n\ntry:\n    from urllib.request import Request, urlopen\n    from urllib.error import HTTPError\n    from urllib.parse import quote, urljoin, urlparse\n    HAVE_URLLIB = True\nexcept ImportError:\n    HAVE_URLLIB = False\n\ntry:\n    from html import unescape as html_unescape\n    HAVE_HTML = True\nexcept ImportError:\n    HAVE_HTML = False\n\nSAFE_URL_CHARS = \"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789_.-~/\"\nUrlParts = namedtuple(\"UrlParts\", [\"scheme\", \"netloc\", \"path\", \"params\", \"query\", \"fragment\"])\nHTML_ENTITIES = {\"amp\": \"&\", \"lt\": \"<\", \"gt\": \">\", \"quot\": '\"', \"apos\": \"'\", \"nbsp\": \" \",\n                 \"#39\": \"'\", \"#x27\": \"'\", \"#x2F\": \"/\", \"#47\": \"/\", \"ndash\": \"-\", \"mdash\": \"-\",\n                 \"hellip\": \"...\", \"copy\": \"(c)\", \"reg\": \"(R)\", \"trade\": \"(TM)\", \"laquo\": \"<<\",\n                 \"raquo\": \">>\", \"lsquo\": \"'\", \"rsquo\": \"'\", \"ldquo\": '\"', \"rdquo\": '\"'}\n\nif not HAVE_URLLIB:\n    def quote(text, safe=\"/\"):\n        out = []\n        for ch in str(text):\n            if ch in SAFE_URL_CHARS or ch in safe:\n                out.append(ch)\n            else:\n                for b in ch.encode(\"utf-8\"):\n                    out.append(\"%%%02X\" % b)\n        return \"\".join(out)\n\n    def urlparse(url):\n        m = re.match(r\"^(?:([a-zA-Z][a-zA-Z0-9+.-]*):)?(?://([^/?#]*))?([^?#]*)(?:\\?([^#]*))?(?:#(.*))?$\", str(url or \"\"))\n        if not m:\n            return UrlParts(\"\", \"\", str(url or \"\"), \"\", \"\", \"\")\n        return UrlParts(m.group(1) or \"\", m.group(2) or \"\", m.group(3) or \"\", \"\", m.group(4) or \"\", m.group(5) or \"\")\n\n    def urljoin(base, href):\n        href = str(href or \"\")\n        if re.match(r\"^[a-zA-Z][a-zA-Z0-9+.-]*:\", href):\n            return href\n        b = urlparse(base)\n        origin = \"%s://%s\" % (b.scheme, b.netloc)\n        if href.startswith(\"//\"):\n            return \"%s:%s\" % (b.scheme, href)\n        if href.startswith(\"/\"):\n            return origin + href\n        base_dir = b.path.rsplit(\"/\", 1)[0] + \"/\" if \"/\" in b.path else \"/\"\n        return origin + base_dir + href\n\nif not HAVE_HTML:\n    def html_unescape(text):\n        def repl(m):\n            name = m.group(1)\n            if name in HTML_ENTITIES:\n                return HTML_ENTITIES[name]\n            if name.startswith(\"#x\") or name.startswith(\"#X\"):\n                try:\n                    return chr(int(name[2:], 16))\n                except ValueError:\n                    return m.group(0)\n            if name.startswith(\"#\"):\n                try:\n                    return chr(int(name[1:]))\n                except ValueError:\n                    return m.group(0)\n            return m.group(0)\n        return re.sub(r\"&(#?[xX]?[0-9A-Za-z]+);\", repl, str(text or \"\"))\n\n\ndef parse_repo(url):\n    m = re.search(r\"github\\.com[/:]+([^/\\s]+)/([^/\\s#?]+)\", url or \"\", re.I)\n    return (m.group(1), m.group(2).removesuffix(\".git\")) if m else None\n\n\ndef make_gh(token):\n    \"\"\"Direct mode: get(url) -> (status, body). Status 0 means the request never completed.\"\"\"\n    tok = (token or \"\").strip()\n\n    def get(url):\n        headers = {\"User-Agent\": UA, \"Accept\": \"application/vnd.github+json\"}\n        if tok and any(h in url for h in GITHUB_HOSTS):\n            headers[\"Authorization\"] = \"Bearer \" + tok\n        attempt = 0\n        while attempt < 3:\n            try:\n                with urlopen(Request(url, headers=headers), timeout=45) as r:\n                    return r.status, r.read().decode(\"utf-8\", \"replace\")\n            except HTTPError as e:\n                body = \"\"\n                try:\n                    body = e.read().decode(\"utf-8\", \"replace\")\n                except Exception:\n                    body = \"\"\n                if e.code in RETRY_STATUSES and attempt < 2:\n                    time.sleep(1.5 * (attempt + 1))\n                    attempt = attempt + 1\n                    continue\n                return e.code, body\n            except Exception as e:\n                if attempt < 2:\n                    time.sleep(1.0)\n                    attempt = attempt + 1\n                    continue\n                return 0, str(e)\n        return 0, \"\"\n\n    return get\n\n\ndef make_cache_gh(cache, missing):\n    \"\"\"Replay mode: serve (status, body) from `cache` {url: [status, body]}; record misses.\n    A miss answers status 0 so the evaluator's own fail-closed paths run unchanged; the\n    driver turns any recorded miss into a `need_fetch` reply instead of a verdict.\"\"\"\n    def get(url):\n        hit = cache.get(url)\n        if hit is None:\n            if url not in missing:\n                missing.append(url)\n            return 0, \"\"\n        return int(hit[0]), str(hit[1] or \"\")\n\n    return get\n\n\n\"\"\"Deterministic RocketRide-usage evaluation engine.\n\nMeasures usage from a repo's CODE (parsed `.pipe` files + source scan), scores it against the\napproved weights (SCORING_SPEC.md), and derives Tag + Backbone deterministically - no LLM. The\nLLM (elsewhere) only writes the human-readable prose from this evaluation.\n\nSplit into:\n  • pure functions  - parse_pipe / sdk_metrics / pipe_called / evaluate  (network-free, unit-tested)\n  • gather(url, gh) - fetches the repo (given a `gh(url)->(status, text)` fetcher) into `evidence`\n\"\"\"\nimport json\nimport re\nROCKETRIDE = target_from_preset(json.loads(PRESET_JSON))\nW = {'dependency': 1.0, 'pipeline_called': 2.0, 'agent_node': 1.0, 'complexity_mid': 0.5, 'complexity_high': 1.0, 'tool_or_llm': 0.5, 'hosted': 1.0, 'sdk_callsites': 0.5, 'file_spread': 0.5, 'uncalled_penalty': 1.0, 'predates_penalty': 1.0}\nTHRESHOLDS = {'significant': 4.0, 'moderate': 2.0, 'less': 1.0}\nhj_MAX_SCORED_PIPES = 6\nEVENT_GRACE_DAYS = 2\nDEFAULT_HISTORY_PENALTY = 2.0\nhj_SDK_CALL = re.compile('\\\\b(RocketRideClient|client\\\\.use|client\\\\.chat|client\\\\.send|client\\\\.connect)', re.I)\nhj_MANIFEST = re.compile('(^|/)(package\\\\.json|requirements\\\\.txt|pyproject\\\\.toml|Cargo\\\\.toml|go\\\\.mod|pom\\\\.xml|build\\\\.gradle(\\\\.kts)?|Gemfile|composer\\\\.json|[^/]+\\\\.csproj)$', re.I)\nhj_SRC_EXT = re.compile('\\\\.(ts|tsx|js|jsx|mjs|cjs|py|rs|go|java|kt|rb|php|cs|swift|scala|sh)$', re.I)\nhj_SRC_HINT = re.compile('rocket|pipeline|integration|agent|engine|config|api|route|server|main|index|app|relay|deploy', re.I)\nhj_INLINE_PIPE = re.compile('client\\\\.use\\\\(\\\\s*\\\\{\\\\s*pipeline\\\\b', re.I)\nhj_RUNNER = re.compile('client\\\\.use\\\\(\\\\s*\\\\{?\\\\s*(?:file)?path\\\\s*[=:]\\\\s*(?![\\'\"])', re.I)\nhj_RR_HTTP = re.compile('api\\\\.rocketride\\\\.ai|rocketride\\\\.ai/[\\\\w/-]*(pipeline|webhook|hook|run|invoke)', re.I)\nhj_HOSTED_ENV = re.compile('rocketride_\\\\w*(url|webhook|endpoint|hook)', re.I)\nhj_PIPE_HINT = re.compile('^(webhook|chat|prompt|response|source|memory|telegram)$|^(llm_|agent_|tool_|db_|embedding_|vector|response_|source_)', re.I)\nhj_JSON_PIPE_PATH = re.compile('pipeline|rocketride', re.I)\nhj_JSON_CONFIG = re.compile('(package(-lock)?|tsconfig|composer|schema|manifest|settings|config)\\\\.json$|node_modules', re.I)\nhj_MANIFEST_CAP = 24\nhj_RR_IMPORT = re.compile('(?:from|import)\\\\s+[\\'\"]?rocketride|import\\\\s*\\\\(\\\\s*[\\'\"]rocketride[\\'\"]', re.I)\nhj_INLINE_PIPE_PATH = '<in-code pipeline>'\n\ndef parse_pipe(text):\n    \"\"\"Node metrics for one .pipe JSON. None if unparseable.\"\"\"\n    try:\n        d = json.loads(text)\n    except Exception:\n        return None\n    if not isinstance(d, dict):\n        return None\n    if isinstance(d.get('pipeline'), dict):\n        d = d['pipeline']\n    comps = d.get('components') or d.get('nodes') or []\n    providers = [str(c.get('provider') or c.get('type') or '') for c in comps]\n    return {'nodes': len(comps), 'providers': providers, 'has_agent': any((p.startswith('agent_') for p in providers)), 'has_llm': any((p.startswith('llm_') for p in providers)), 'tool_count': sum((p.startswith('tool_') for p in providers)), 'project_id': d.get('project_id') or ''}\n\ndef complexity_band(nodes):\n    return 'high' if nodes >= 9 else 'medium' if nodes >= 4 else 'low'\n\ndef history_tamper_scan(commits, win):\n    \"\"\"Scan a repo's commit list for evidence of history rewriting. Git stamps every commit with an\n    AUTHOR date (when the work was originally made) and a COMMITTER date (when it was last\n    rewritten). A commit whose committer date sits in/after the event window while its author date\n    is OLDER than the window is pre-existing work re-stamped for the event (rebase / amend /\n    filter-branch) - git's own metadata records the rewrite.\n    Returns ([tampered commits], earliest_date_seen).\"\"\"\n    tampered, earliest = ([], '')\n    for it in commits or []:\n        c = (it or {}).get('commit', {})\n        a = (c.get('author') or {}).get('date', '') or ''\n        m = (c.get('committer') or {}).get('date', '') or ''\n        for d in (a, m):\n            if d and (not earliest or d < earliest):\n                earliest = d\n        if a and m and (a[:10] < win['start']) and (m[:10] >= win['start']):\n            tampered.append({'sha': (it.get('sha') or '')[:7], 'author': a, 'committer': m})\n    return (tampered[:5], earliest)\n\ndef event_window(event_date):\n    \"\"\"The allowed commit window for an event date (YYYY-MM-DD): event ± EVENT_GRACE_DAYS.\n    Pipelines whose history starts BEFORE the window are pre-existing work (reuse).\"\"\"\n    if not event_date:\n        return None\n    try:\n        from datetime import date, timedelta\n        d = date.fromisoformat(str(event_date)[:10])\n    except ValueError:\n        return None\n    g = timedelta(days=EVENT_GRACE_DAYS)\n    return {'event': d.isoformat(), 'start': (d - g).isoformat(), 'end': (d + g).isoformat()}\n\ndef hj_looks_like_pipeline(m):\n    \"\"\"True only if a parsed JSON is actually a pipeline (has provider-bearing nodes) - so a\n    package.json / config file is never mistaken for one.\"\"\"\n    return bool(m) and m['nodes'] > 0 and any((hj_PIPE_HINT.search(p) for p in m['providers'] if p))\nhj_INVOKE = ('client.use', 'client.send', 'client.chat', 'rocketride start')\nhj_TECH_EXT = {'.py': 'Python', '.ts': 'TypeScript', '.tsx': 'TypeScript', '.js': 'JavaScript', '.jsx': 'JavaScript', '.mjs': 'JavaScript', '.rs': 'Rust', '.go': 'Go', '.java': 'Java', '.kt': 'Kotlin', '.rb': 'Ruby', '.php': 'PHP', '.cs': 'C#', '.swift': 'Swift', '.scala': 'Scala', '.vue': 'Vue', '.svelte': 'Svelte', '.sh': 'Shell'}\nhj_TECH_FRAMEWORKS = (('\"next\"', 'Next.js'), ('\"react\"', 'React'), ('\"vite\"', 'Vite'), ('\"tailwindcss\"', 'Tailwind CSS'), ('\"express\"', 'Express'), ('\"vue\"', 'Vue'), ('\"svelte\"', 'Svelte'), ('fastapi', 'FastAPI'), ('flask', 'Flask'), ('django', 'Django'), ('streamlit', 'Streamlit'), ('gradio', 'Gradio'), ('\"electron\"', 'Electron'))\n\ndef detect_tech(paths, manifest_texts, gh_languages=None):\n    \"\"\"Language + framework chips for a repo. Languages come from GitHub's own Linguist\n    detection (the /languages API - authoritative, same data as the repo's language bar)\n    when available, ranked by byte count; extension counting is only the offline fallback.\n    Frameworks come from manifest contents (Linguist doesn't know frameworks).\"\"\"\n    if gh_languages:\n        langs = [k for k, _ in sorted(gh_languages.items(), key=lambda kv: -kv[1])][:5]\n    else:\n        counts = {}\n        for p in paths:\n            pl = p.lower()\n            if 'node_modules/' in pl or 'vendor/' in pl or '/dist/' in pl:\n                continue\n            dot = pl.rfind('.')\n            t = hj_TECH_EXT.get(pl[dot:]) if dot >= 0 else None\n            if t:\n                counts[t] = counts.get(t, 0) + 1\n        langs = [t for t, _ in sorted(counts.items(), key=lambda kv: -kv[1])][:5]\n    joined = ' '.join(manifest_texts).lower()\n    fws = [label for key, label in hj_TECH_FRAMEWORKS if key in joined and label not in langs]\n    return langs + fws[:5]\n\ndef hj_gh_languages(gh, owner, repo):\n    \"\"\"GitHub Linguist language byte counts, or None if the call fails (fallback kicks in).\"\"\"\n    st, body = gh(f'https://api.github.com/repos/{owner}/{repo}/languages')\n    if st != 200:\n        return None\n    try:\n        d = json.loads(body)\n        return d if isinstance(d, dict) and d else None\n    except Exception:\n        return None\n\ndef hj_first_site(source_files, rx, snippet):\n    \"\"\"First line matching `rx` across the source, as a call-site dict (for inline / runner patterns).\"\"\"\n    for f in source_files:\n        mt = rx.search(f['text'])\n        if mt:\n            return {'file': f['path'], 'line': f['text'][:mt.start()].count('\\n') + 1, 'snippet': snippet}\n    return None\n\ndef hj_manifest_paths(paths, cap=hj_MANIFEST_CAP):\n    \"\"\"Manifest blobs in fetch-priority order — root first, then workspace packages (monorepos).\"\"\"\n    mfs = [p for p in paths if hj_MANIFEST.search(p)]\n\n    def rank(p):\n        pl = p.lower()\n        segs = p.split('/')\n        is_root = len(segs) == 1\n        has_rr = 'rocketride' in pl\n        in_workspace = any((s in ('packages', 'apps', 'libs', 'crates') for s in segs[:-1]))\n        return (0 if is_root else 1, 0 if has_rr else 1, 0 if in_workspace else 1, len(segs), pl)\n    return sorted(mfs, key=rank)[:cap]\n\ndef hj_infer_inline_pipeline_metrics(source_files):\n    \"\"\"Best-effort node metrics when the only pipeline is compiled/hand-built in code.\"\"\"\n    joined = '\\n'.join((f['text'] for f in source_files))\n    providers = []\n    for m in re.finditer('provider\\\\s*[:=]\\\\s*[\\'\"]([^\\'\"]+)[\\'\"]', joined, re.I):\n        providers.append(m.group(1))\n    for m in re.finditer('[\\'\"](agent_[^\\'\"]+|llm_[^\\'\"]+|tool_[^\\'\"]+)[\\'\"]', joined):\n        providers.append(m.group(1))\n    for const in ('webhook', 'response_text', 'response_answers', 'chat', 'prompt', 'ner'):\n        if re.search(f\"\"\"['\\\\\"]{const}['\\\\\"]\"\"\", joined, re.I):\n            providers.append(const)\n    providers = list(dict.fromkeys((p for p in providers if p)))\n    nodes = len(providers)\n    if nodes < 4 and ('compileToRocketRide' in joined or hj_INLINE_PIPE.search(joined)):\n        nodes = max(nodes, 5)\n    if nodes == 0:\n        nodes = 5\n    has_agent = any((str(p).startswith('agent_') for p in providers))\n    has_llm = any((str(p).startswith('llm_') for p in providers))\n    return {'nodes': nodes, 'providers': providers[:20], 'has_agent': has_agent, 'has_llm': has_llm, 'tool_count': sum((1 for p in providers if str(p).startswith('tool_'))), 'project_id': ''}\n\ndef hj_call_sites(f, base):\n    out = []\n    for i, line in enumerate(f['text'].splitlines(), 1):\n        ll = line.lower()\n        if base in ll or any((k in ll for k in hj_INVOKE)):\n            out.append({'file': f['path'], 'line': i, 'snippet': line.strip()[:110]})\n    return out\n\ndef pipe_called(pipe_path, source_files):\n    \"\"\"A pipeline is 'called' when the code loads/runs it via the SDK. Two patterns count:\n      • strong     - a file names the pipe (its basename appears, even inside resolve(HERE, '...'))\n                     AND that same file invokes client.use/send/chat / `rocketride start`;\n      • cross-file - the basename is referenced in one file while the SDK is invoked in another\n                     (common when the path is a shared constant or built dynamically).\n    Returns (called, call_sites[]).\"\"\"\n    base = pipe_path.rsplit('/', 1)[-1].lower()\n    ref_files = [f for f in source_files if base in f['text'].lower()]\n    if not ref_files:\n        return (False, [])\n    for f in ref_files:\n        if any((k in f['text'].lower() for k in hj_INVOKE)):\n            return (True, hj_call_sites(f, base)[:6])\n    if any((k in f['text'].lower() for f in source_files for k in hj_INVOKE)):\n        sites = [s for f in ref_files for s in hj_call_sites(f, base)]\n        return (True, sites[:6])\n    return (False, [])\n\ndef sdk_metrics(source_files):\n    \"\"\"Deterministic SDK-depth metrics across the fetched source.\"\"\"\n    callsites = files_using = 0\n    hosted = engine = False\n    for f in source_files:\n        t, low = (f['text'], f['text'].lower())\n        n = len(hj_SDK_CALL.findall(t))\n        callsites += n\n        if n or hj_RR_IMPORT.search(low) or '@rocketride' in low:\n            files_using += 1\n        if 'rocketride_pipeline' in low or 'lib/rocketride' in low or hj_HOSTED_ENV.search(low):\n            hosted = True\n        if 'ws://localhost:5565' in low or '/v1/pipelines' in low or hj_RR_HTTP.search(low):\n            engine = True\n    return {'callsites': callsites, 'file_spread': files_using, 'hosted': hosted, 'engine': engine}\nhj_AI_COMPETITORS = {'langchain', 'crewai'}\n\ndef hj_backbone(pipelines, sdk, evidence):\n    runs_orchestration = any((p['called'] and (p.get('has_agent') or p.get('has_llm')) for p in pipelines))\n    real = runs_orchestration or sdk['hosted'] or sdk['engine']\n    if not real:\n        return 'No'\n    if any((o in hj_AI_COMPETITORS for o in evidence.get('other_platforms', []))):\n        return 'Partial'\n    return 'Yes'\n\ndef hj_tag(score, backbone, called, evidence):\n    if evidence.get('overclaim') or not evidence.get('accessible', True):\n        return 'None'\n    if evidence.get('scaffold_only') and called == 0:\n        return 'Less'\n    if score >= THRESHOLDS['significant']:\n        return 'Significant' if backbone in ('Yes', 'Partial') else 'Moderate'\n    if score >= THRESHOLDS['moderate']:\n        return 'Moderate'\n    if score >= THRESHOLDS['less']:\n        return 'Less'\n    return 'None'\n\ndef evaluate(evidence, target=None):\n    \"\"\"Score the gathered evidence → deterministic Tag / Backbone / ground-truth table.\n    `target=None` (or the pipeline-scoring preset) uses the legacy RocketRide path unchanged;\n    any other target routes to the target-agnostic generic path.\"\"\"\n    if target is not None and (not target.pipeline_scoring):\n        return hj_evaluate_generic(evidence, target)\n    if not evidence.get('accessible'):\n        return {'tag': 'None', 'backbone': 'No', 'score': 0.0, 'pipelines': [], 'sdk': {}, 'breakdown': [], 'pipelines_called': 0, 'reason': 'inaccessible', 'scoring': 'pipeline'}\n    breakdown, acc = ([], [0.0])\n\n    def add(label, pts):\n        acc[0] = acc[0] + pts\n        breakdown.append({'signal': label, 'points': pts})\n    if evidence.get('dependency'):\n        add('dependency in manifest', W['dependency'])\n    sdk = evidence.get('sdk', {'callsites': 0, 'file_spread': 0, 'hosted': False, 'engine': False})\n    pipelines = []\n    for p in evidence.get('pipes', []):\n        m = p.get('metrics')\n        nodes = m['nodes'] if m else 0\n        pipelines.append({'name': p['path'], 'nodes': nodes, 'complexity': complexity_band(nodes), 'has_agent': bool(m and m['has_agent']), 'has_llm': bool(m and m['has_llm']), 'tool_count': m['tool_count'] if m else 0, 'providers': m['providers'] if m else [], 'called': p.get('called', False), 'call_sites': p.get('call_sites', []), 'first_commit': p.get('first_commit')})\n    called_pipes = [e for e in pipelines if e['called']]\n    called = len(called_pipes)\n    repo_invokes = called > 0 or sdk.get('callsites', 0) > 0 or bool(sdk.get('engine'))\n    for e in called_pipes[:hj_MAX_SCORED_PIPES]:\n        add(f\"pipeline called: {e['name']}\", W['pipeline_called'])\n        if e['has_agent']:\n            add(f\"agent node in {e['name']}\", W['agent_node'])\n        if e['nodes'] >= 9:\n            add(f\"complexity high ({e['nodes']} nodes)\", W['complexity_high'])\n        elif e['nodes'] >= 4:\n            add(f\"complexity mid ({e['nodes']} nodes)\", W['complexity_mid'])\n        if e['has_llm'] or e['tool_count']:\n            add(f\"tool/llm node in {e['name']}\", W['tool_or_llm'])\n    if called > hj_MAX_SCORED_PIPES:\n        add(f'(+{called - hj_MAX_SCORED_PIPES} more called pipeline(s) - score capped)', 0.0)\n    if not repo_invokes:\n        for e in pipelines:\n            if not e['called']:\n                add(f\"PENALTY pipeline never called: {e['name']}\", -W['uncalled_penalty'])\n    win = evidence.get('event_window')\n    reused = []\n    if win:\n        for e in pipelines:\n            e['predates'] = bool(e['called'] and e.get('first_commit') and (str(e['first_commit'])[:10] < win['start']))\n        reused = [e for e in pipelines if e.get('predates')]\n        if reused:\n            names = ', '.join((f\"{e['name']} (first commit {str(e['first_commit'])[:10]})\" for e in reused[:4]))\n            add(f\"PENALTY: pipeline predates event window {win['start']}..{win['end']} - {names}\", -W['predates_penalty'])\n    if sdk.get('hosted'):\n        add('hosted-pipeline usage', W['hosted'])\n    if sdk.get('callsites', 0) >= 3:\n        add(f\"SDK call-sites ({sdk['callsites']})\", W['sdk_callsites'])\n    if sdk.get('file_spread', 0) >= 2:\n        add(f\"SDK spread ({sdk['file_spread']} files)\", W['file_spread'])\n    project_predates = evidence.get('project_predates')\n    tampered = evidence.get('history_tampered') or []\n    pen = evidence.get('history_penalty')\n    pen = DEFAULT_HISTORY_PENALTY if pen is None else max(0.0, float(pen))\n    if win and project_predates:\n        pp_date = str(project_predates.get('date', ''))[:10]\n        early = str(evidence.get('earliest_commit', ''))[:10]\n        earliest = min((d for d in (pp_date, early) if d)) if pp_date or early else '?'\n        add(f\"⚠ FLAGGED: project history predates the event window {win['start']}..{win['end']} - work goes back to {earliest} (latest pre-window commit {project_predates.get('sha', '?')} on {pp_date or '?'}); judge-set penalty\", -pen)\n    if win and tampered:\n        t0 = tampered[0]\n        add(f\"⚠ FLAGGED: commit-date rewrite detected - {t0['sha']} authored {str(t0['author'])[:10]} but re-stamped {str(t0['committer'])[:10]} into the window; judge-set penalty\", -pen)\n    score = max(0.0, round(acc[0], 1))\n    backbone = hj_backbone(pipelines, sdk, evidence)\n    tag = hj_tag(score, backbone, called, evidence)\n    if tag in ('None', 'Less'):\n        backbone = 'No'\n    return {'tag': tag, 'backbone': backbone, 'score': score, 'pipelines': pipelines, 'sdk': sdk, 'breakdown': breakdown, 'pipelines_called': called, 'tech': evidence.get('tech', []), 'pipelines_total': len(pipelines), 'other_platforms': evidence.get('other_platforms', []), 'scoring': 'pipeline', 'event_window': win, 'history_penalty': pen if win else None, 'project_predates': project_predates if win else None, 'history_tampered': tampered if win else [], 'earliest_commit': evidence.get('earliest_commit', ''), 'repo_created_at': evidence.get('repo_created_at', ''), 'reused_pipelines': [{'name': e['name'], 'first_commit': e.get('first_commit')} for e in reused]}\nZERO_EVAL = {'score': 0.0, 'pipelines': [], 'breakdown': [], 'pipelines_called': 0, 'pipelines_total': 0, 'other_platforms': [], 'event_window': None, 'reused_pipelines': [], 'project_predates': None, 'history_tampered': [], 'earliest_commit': '', 'repo_created_at': '', 'history_penalty': None, 'tech': []}\nEXPLAIN_PROMPT = 'You are documenting how a hackathon project used RocketRide, for a judge.\\n\\nThe VERDICT has ALREADY been decided deterministically from the project\\'s actual code - you must NOT\\nchange it. Your only job is to explain it in plain English, grounded entirely in the evidence table\\nyou are given (pipelines, their node counts, and whether each is actually CALLED in the code, with\\ncall sites).\\n\\nReturn ONLY a strict JSON object - start with { and end with }, no prose outside it:\\n{\\n  \"description\": \"<1-2 sentences: what the project does>\",\\n  \"rocketride_usage\": \"<1-2 sentences: concretely how it uses RocketRide - name the pipeline(s), the\\n                        node count, and whether/where they are called; ground every claim in the table>\",\\n  \"justification\": \"<2-3 sentences: why the code earns THIS tag and backbone, citing the table (e.g.\\n                     \\'the 7-node agent pipeline is loaded and run at relay.ts:83\\'). If a pipeline is\\n                     present but never called, say so explicitly. If a pipeline predates the event\\n                     window (reused), call that out.>\",\\n  \"project_name\": \"<ONLY if the README clearly states the project\\'s name - else omit>\",\\n  \"team_members\": \"<ONLY if the README clearly lists team member names - comma-separated - else omit>\"\\n}\\nRules: cite ONLY what is in the evidence. Never contradict the verdict. If the tag is None, state that\\nthe code shows no real RocketRide usage. Write plainly and never use em dashes anywhere in your\\nprose; use commas, periods, or parentheses instead.'\n\ndef hj_iter_json_objects(text):\n    depth, start = (0, None)\n    for i, ch in enumerate(text):\n        if ch == '{':\n            if depth == 0:\n                start = i\n            depth += 1\n        elif ch == '}' and depth > 0:\n            depth -= 1\n            if depth == 0 and start is not None:\n                yield text[start:i + 1]\n                start = None\n\ndef extract_prose(text):\n    \"\"\"Pull the {description, rocketride_usage, justification} object out of the LLM reply, tolerating\n    any wrapping analysis. The verdict is NOT here - that's decided deterministically.\"\"\"\n    best = {}\n    for cand in hj_iter_json_objects(text or ''):\n        try:\n            obj = json.loads(cand)\n        except Exception:\n            continue\n        if isinstance(obj, dict) and any((k in obj for k in ('description', 'rocketride_usage', 'justification'))):\n            best = obj\n    return best\n\ndef explain_prompt(evaluation, project, repo, feedback, readme_head='', target_name='RocketRide'):\n    \"\"\"Build the prose prompt: the deterministic verdict + the ground-truth table as fixed context.\n    `target_name` retargets every display-name mention (the wire key `rocketride_usage` is\n    lowercase and untouched, so parsing never changes).\"\"\"\n    payload = {'verdict': {'tag': evaluation['tag'], 'backbone': evaluation['backbone'], 'score': evaluation['score']}, 'score_breakdown': evaluation.get('breakdown', []), 'pipelines': [{'name': p['name'], 'nodes': p['nodes'], 'complexity': p['complexity'], 'has_agent': p['has_agent'], 'called': p['called'], 'first_commit': p.get('first_commit'), 'call_sites': [f\"{s['file']}:{s['line']}\" for s in p.get('call_sites', [])]} for p in evaluation.get('pipelines', [])], 'sdk': evaluation.get('sdk', {}), 'other_platforms': evaluation.get('other_platforms', []), 'event_window': evaluation.get('event_window'), 'reused_pipelines': evaluation.get('reused_pipelines', [])}\n    if evaluation.get('platform'):\n        payload['platform_evidence'] = evaluation['platform']\n    readme_block = f'\\n\\nREADME EXCERPT (for project_name / team_members / description only):\\n{readme_head[:2500]}' if readme_head else ''\n    base = EXPLAIN_PROMPT if target_name == 'RocketRide' else EXPLAIN_PROMPT.replace('RocketRide', target_name)\n    return base + f'\\n\\nPROJECT: {project}\\nREPO: {repo}\\n' + 'TEAM FEEDBACK (context only - the verdict is already fixed from code): ' + f\"{feedback or '(none provided)'}\\n\\nDETERMINISTIC EVALUATION (do not change the verdict):\\n\" + json.dumps(payload, indent=2) + readme_block\n\ndef evidence_lines(ev):\n    \"\"\"Deterministic, judge-readable evidence bullets built straight from the metrics (no LLM).\"\"\"\n    lines = []\n    win = ev.get('event_window')\n    pen = ev.get('history_penalty')\n    pen_txt = f'−{pen:g} judge-set penalty' if pen else 'flag only - no deduction'\n    if win and ev.get('project_predates'):\n        pp = ev['project_predates']\n        pp_date = str(pp.get('date', ''))[:10]\n        early = str(ev.get('earliest_commit', ''))[:10]\n        earliest = min((d for d in (pp_date, early) if d)) if pp_date or early else '?'\n        lines.append(f\"⚠ FLAGGED - project history predates the event window {win['start']}..{win['end']}: work goes back to {earliest}; latest pre-window commit {pp.get('sha', '?')} on {pp_date or '?'} ({pen_txt}; judge's call)\")\n    if win and ev.get('history_tampered'):\n        for t in ev['history_tampered'][:3]:\n            lines.append(f\"⚠ FLAGGED - commit-date rewrite: {t['sha']} authored {str(t['author'])[:10]} but re-stamped {str(t['committer'])[:10]} ({pen_txt}; judge's call)\")\n    if win and ev.get('reused_pipelines'):\n        for r in ev['reused_pipelines']:\n            lines.append(f\"⚠ REUSED PIPELINE: {r['name']} first committed {str(r.get('first_commit') or '?')[:10]} - BEFORE the event window {win['start']}..{win['end']} (−1 penalty)\")\n    for p in ev.get('pipelines', []):\n        where = '; '.join((f\"{s['file']}:{s['line']}\" for s in p.get('call_sites', [])[:3]))\n        status = f'CALLED @ {where}' if p['called'] and where else 'CALLED' if p['called'] else 'NOT called (present for show)'\n        fc = f\", first commit {str(p['first_commit'])[:10]}\" if p.get('first_commit') else ''\n        lines.append(f\"{p['name']} - {p['nodes']} nodes ({p['complexity']}), {('agent' if p['has_agent'] else 'no agent')} - {status}{fc}\")\n    sdk = ev.get('sdk', {})\n    lines.append(f\"SDK: {sdk.get('callsites', 0)} call-site(s) across {sdk.get('file_spread', 0)} file(s)\" + (', hosted/Cloud usage' if sdk.get('hosted') else ''))\n    pf = ev.get('platform') or {}\n    if pf.get('domains'):\n        lines.append('Deployed on target platform: ' + ', '.join(pf['domains'][:3]))\n    if pf.get('files'):\n        lines.append('Target config/artifacts committed: ' + ', '.join(pf['files'][:3]))\n    if pf.get('markers'):\n        lines.append('Account/env markers: ' + ', '.join(pf['markers'][:4]))\n    if ev.get('other_platforms'):\n        lines.append('Other platforms in code: ' + ', '.join(ev['other_platforms']))\n    return lines\n\ndef det_note(ev):\n    if ev.get('scoring') == 'generic':\n        hits = [b['signal'] for b in ev.get('breakdown') or [] if (b.get('points') or 0) > 0]\n        note = f\"Deterministic score {ev['score']} -> {ev['tag']} / backbone {ev['backbone']} (SDK & platform signals{(': ' + '; '.join(hits[:4]) if hits else '')}).\"\n    else:\n        note = f\"Deterministic score {ev['score']} -> {ev['tag']} / backbone {ev['backbone']}; {ev['pipelines_called']}/{ev['pipelines_total']} pipeline(s) called.\"\n    pen = ev.get('history_penalty')\n    pen_txt = f'−{pen:g}' if pen else 'flag only'\n    if ev.get('project_predates'):\n        pp_date = str(ev['project_predates'].get('date', ''))[:10]\n        early = str(ev.get('earliest_commit', ''))[:10]\n        earliest = min((d for d in (pp_date, early) if d)) if pp_date or early else '?'\n        note += f\" ⚠ FLAGGED: project history predates the event window (work goes back to {earliest}; {pen_txt}, judge's call).\"\n    if ev.get('history_tampered'):\n        t0 = ev['history_tampered'][0]\n        note += f\" ⚠ FLAGGED: commit-date rewrite detected ({t0['sha']}: authored {str(t0['author'])[:10]}, re-stamped {str(t0['committer'])[:10]}; {pen_txt}, judge's call).\"\n    if ev.get('reused_pipelines'):\n        names = ', '.join((r['name'] for r in ev['reused_pipelines'][:3]))\n        note += f' ⚠ REUSED PIPELINE(S) predating the event window: {names} (−1).'\n    return note\n\ndef gather(url, gh, event_date=None, history_penalty=None, target=None):\n    \"\"\"Fetch a repo into an `evidence` dict. `gh(url) -> (status, text)`.\n    When `event_date` (YYYY-MM-DD) is given, each pipeline's first-commit date is fetched so\n    evaluate() can flag pre-existing (reused) pipelines against the event ± grace-day window.\n    `history_penalty` is the judge-set deduction for project-predates / tampered-history flags\n    (None -> DEFAULT_HISTORY_PENALTY; 0 = flag only). `target=None` / the preset = legacy\n    RocketRide gathering; other targets use the target-agnostic generic gatherer.\"\"\"\n    if target is not None and (not target.pipeline_scoring):\n        return hj_gather_generic(url, gh, event_date, history_penalty, target)\n    pr = parse_repo(url)\n    if not pr:\n        return {'accessible': False, 'note': 'URL not parseable'}\n    owner, repo = pr\n    st, body = gh(f'https://api.github.com/repos/{owner}/{repo}')\n    if st != 200:\n        return {'accessible': False, 'status': st}\n    meta = json.loads(body)\n    branch = meta.get('default_branch', 'main')\n    created_at = meta.get('created_at', '')\n    st, tbody = gh(f'https://api.github.com/repos/{owner}/{repo}/git/trees/{branch}?recursive=1')\n    if st != 200:\n        return {'accessible': True, 'fetch_incomplete': True, 'note': f'tree fetch failed (HTTP {st})'}\n    tree = json.loads(tbody)\n    if tree.get('truncated') is True:\n        return {'accessible': True, 'fetch_incomplete': True, 'note': 'GitHub recursive tree was truncated — no verdict on partial retrieval'}\n    paths = [x.get('path', '') for x in tree.get('tree', []) if x.get('type') == 'blob']\n    fetch_fails = [0]\n\n    def raw(p):\n        st_f, body_f = gh(f'https://raw.githubusercontent.com/{owner}/{repo}/{branch}/{quote(p)}')\n        if st_f != 200:\n            fetch_fails[0] = fetch_fails[0] + 1\n            return ''\n        return body_f\n    pipes = [{'path': pp, 'metrics': parse_pipe(raw(pp))} for pp in [p for p in paths if p.endswith('.pipe')][:12]]\n    for jp in [p for p in paths if p.endswith('.json') and hj_JSON_PIPE_PATH.search(p) and (not hj_JSON_CONFIG.search(p))][:6]:\n        m = parse_pipe(raw(jp))\n        if hj_looks_like_pipeline(m):\n            pipes.append({'path': jp, 'metrics': m})\n    dependency, others = (False, set())\n    manifest_texts = []\n    for mf in hj_manifest_paths(paths):\n        txt = raw(mf)\n        manifest_texts.append(txt)\n        if re.search('rocketride|@rocketride/sdk', txt, re.I):\n            dependency = True\n        others.update((o for o in OTHER_PLATFORMS if o in txt.lower()))\n    src = [p for p in paths if hj_SRC_EXT.search(p)]\n    rr = [p for p in src if 'rocketride' in p.lower()]\n    kw = [p for p in src if p not in rr and hj_SRC_HINT.search(p)]\n    rest = [p for p in src if p not in rr and p not in kw]\n    source_files = []\n    for cf in (rr + kw + rest)[:40]:\n        txt = raw(cf)\n        source_files.append({'path': cf, 'text': txt})\n        others.update((o for o in OTHER_PLATFORMS if o in txt.lower()))\n    inline_site = hj_first_site(source_files, hj_INLINE_PIPE, 'client.use({ pipeline: … }) - in-code pipeline')\n    runner_site = hj_first_site(source_files, hj_RUNNER, 'client.use(filepath=<variable>) - pipes run via a generic runner')\n    for pe in pipes:\n        called, sites = pipe_called(pe['path'], source_files)\n        if not called and pe['path'].endswith('.json') and inline_site:\n            called, sites = (True, [inline_site])\n        if not called and runner_site:\n            called, sites = (True, [runner_site])\n        pe['called'], pe['call_sites'] = (called, sites)\n    if inline_site and (not any((pe.get('called') for pe in pipes))):\n        pipes.append({'path': hj_INLINE_PIPE_PATH, 'metrics': hj_infer_inline_pipeline_metrics(source_files), 'called': True, 'call_sites': [inline_site], 'first_commit': None})\n    win = event_window(event_date)\n    project_predates, tampered, earliest = (None, [], '')\n    if win:\n        for pe in pipes:\n            st, cbody = gh(f\"https://api.github.com/repos/{owner}/{repo}/commits?path={quote(pe['path'])}&per_page=100\")\n            first = None\n            if st == 200:\n                try:\n                    commits = json.loads(cbody)\n                    if isinstance(commits, list) and commits:\n                        c = commits[-1].get('commit', {})\n                        dates = [c.get('author', {}).get('date', ''), c.get('committer', {}).get('date', '')]\n                        first = min((d for d in dates if d)) if any(dates) else None\n                except Exception:\n                    first = None\n            pe['first_commit'] = first\n        st, cbody = gh(f\"https://api.github.com/repos/{owner}/{repo}/commits?until={win['start']}T00:00:00Z&per_page=1\")\n        if st == 200:\n            try:\n                lst = json.loads(cbody)\n                if isinstance(lst, list) and lst:\n                    c = lst[0].get('commit', {})\n                    dates = [(c.get('author') or {}).get('date', ''), (c.get('committer') or {}).get('date', '')]\n                    d = min((x for x in dates if x), default='')\n                    if d:\n                        project_predates = {'date': d, 'sha': (lst[0].get('sha') or '')[:7]}\n            except Exception:\n                project_predates = None\n        st, cbody = gh(f'https://api.github.com/repos/{owner}/{repo}/commits?per_page=100')\n        if st == 200:\n            try:\n                tampered, earliest = history_tamper_scan(json.loads(cbody), win)\n            except Exception:\n                tampered, earliest = ([], '')\n    readme_title, readme_head = ('', '')\n    readmes = sorted([p for p in paths if re.fullmatch('readme\\\\.(md|rst|txt)', p, re.I)], key=len) or sorted([p for p in paths if p.lower().endswith('/readme.md')], key=len)\n    if readmes:\n        head_lines = raw(readmes[0]).splitlines()[:60]\n        readme_head = '\\n'.join(head_lines)\n        for ln in head_lines:\n            if ln.strip().startswith('# '):\n                readme_title = ln.strip().lstrip('# ').strip()\n                break\n    scaffold = any((p.endswith('.claude/rules/rocketride.md') for p in paths))\n    if fetch_fails[0]:\n        return {'accessible': True, 'fetch_incomplete': True, 'note': f'{fetch_fails[0]} file fetch(es) failed — no verdict on partial retrieval'}\n    return {'accessible': True, 'file_count': len(paths), 'tech': detect_tech(paths, manifest_texts, hj_gh_languages(gh, owner, repo)), 'dependency': dependency, 'pipes': pipes, 'source_files': source_files, 'other_platforms': sorted(others), 'scaffold_only': scaffold and (not (dependency or pipes)), 'sdk': sdk_metrics(source_files), 'event_window': win, 'repo_created_at': created_at, 'history_penalty': history_penalty, 'project_predates': project_predates, 'history_tampered': tampered, 'earliest_commit': earliest, 'readme_title': readme_title, 'readme_head': readme_head}\n\ndef hj_generic_search_tokens(t):\n    \"\"\"Tokens long enough to search committed pipelines without matching every file.\"\"\"\n    seen, out = (set(), [])\n    for tok in tuple(t.import_hints) + tuple(t.invoke_tokens) + (t.slug,):\n        s = str(tok or '').strip().lower()\n        if len(s) < 6 or ' ' in s or s in seen:\n            continue\n        seen.add(s)\n        out.append(s)\n    return out\n\ndef hj_generic_provider_hit(provider, tokens):\n    p = str(provider or '').strip().lower()\n    if not p:\n        return False\n    return any((tok in p for tok in tokens))\n\ndef hj_gather_generic(url, gh, event_date, history_penalty, t):\n    \"\"\"Fetch a repo into evidence for an arbitrary target: dependency in manifests, invocation\n    call-sites, runtime API usage, env/key markers, committed artifacts, deploy-domain links,\n    plus the same project-level commit-history integrity checks as the preset path.\"\"\"\n    pr = parse_repo(url)\n    if not pr:\n        return {'accessible': False, 'note': 'URL not parseable'}\n    owner, repo = pr\n    st, body = gh(f'https://api.github.com/repos/{owner}/{repo}')\n    if st != 200:\n        return {'accessible': False, 'status': st}\n    meta = json.loads(body)\n    branch = meta.get('default_branch', 'main')\n    created_at = meta.get('created_at', '')\n    st, tbody = gh(f'https://api.github.com/repos/{owner}/{repo}/git/trees/{branch}?recursive=1')\n    if st != 200:\n        return {'accessible': True, 'fetch_incomplete': True, 'note': f'tree fetch failed (HTTP {st})'}\n    tree = json.loads(tbody)\n    if tree.get('truncated') is True:\n        return {'accessible': True, 'fetch_incomplete': True, 'note': 'GitHub recursive tree was truncated — no verdict on partial retrieval'}\n    paths = [x.get('path', '') for x in tree.get('tree', []) if x.get('type') == 'blob']\n    fetch_fails = [0]\n\n    def raw(p):\n        st_f, body_f = gh(f'https://raw.githubusercontent.com/{owner}/{repo}/{branch}/{quote(p)}')\n        if st_f != 200:\n            fetch_fails[0] = fetch_fails[0] + 1\n            return ''\n        return body_f\n    artifact_files = [p for p in paths if any((tok in p.lower() for tok in t.artifact_paths + t.platform_files))][:12]\n    own = {t.slug, t.name.lower()} | set(t.import_hints)\n    watch = {w for w in (set(OTHER_PLATFORMS) | set(t.competitors)) - set(t.neutral) if not any((w in o or o in w for o in own))}\n    dependency, others = (False, set())\n    manifest_texts = []\n    for mf in hj_manifest_paths(paths):\n        txt = raw(mf)\n        manifest_texts.append(txt)\n        if t.dependency_rx and t.dependency_rx.search(txt):\n            dependency = True\n        others.update((o for o in watch if o in txt.lower()))\n    src = [p for p in paths if hj_SRC_EXT.search(p)]\n\n    def hj_src_rank(p):\n        pl = p.lower()\n        named_hit = 1 if t.name_in_path_rx and t.name_in_path_rx.search(p) else 0\n        example = 1 if any((seg in pl for seg in ('/examples/', '/example/', '/sdk/', '/foreign/'))) else 0\n        return (-named_hit, -example, len(p), p)\n    ordered = sorted(src, key=hj_src_rank)\n    source_files, callsites, files_using = ([], 0, 0)\n    hosted = api_used = False\n    domain_hits, marker_hits = (set(), set())\n    for cf in ordered[:80]:\n        txt = raw(cf)\n        if not txt.strip():\n            continue\n        low = txt.lower()\n        source_files.append({'path': cf, 'text': txt})\n        n = len(t.sdk_call_rx.findall(txt)) if t.sdk_call_rx else 0\n        callsites += n\n        if n or any((h in low for h in t.import_hints)):\n            files_using += 1\n        if any((tok in low for tok in t.hosted_tokens)):\n            hosted = True\n            marker_hits.update((tok for tok in t.platform_markers if tok in low))\n        if any((tok in low for tok in t.engine_tokens)):\n            api_used = True\n        domain_hits.update((d for d in t.platform_domains if d in low))\n        others.update((o for o in watch if o in low))\n    pipe_tokens = hj_generic_search_tokens(t)\n    pipe_paths = [p for p in paths if p.endswith('.pipe')][:12]\n    for jp in [p for p in paths if p.endswith('.json') and hj_JSON_PIPE_PATH.search(p) and (not hj_JSON_CONFIG.search(p))][:6]:\n        if jp not in pipe_paths:\n            pipe_paths.append(jp)\n    for pp in pipe_paths:\n        txt = raw(pp)\n        if not txt.strip():\n            continue\n        metrics = parse_pipe(txt)\n        if pp.endswith('.json') and (not hj_looks_like_pipeline(metrics)):\n            continue\n        providers = (metrics or {}).get('providers') or []\n        hits = [p for p in providers if hj_generic_provider_hit(p, pipe_tokens)]\n        if not hits and pipe_tokens and any((tok in txt.lower() for tok in pipe_tokens)):\n            hits = ['text']\n        if not hits:\n            continue\n        callsites += len(hits)\n        files_using += 1\n        dependency = True\n        if pp not in artifact_files:\n            artifact_files.append(pp)\n        if any((tok in txt.lower() for tok in t.hosted_tokens)):\n            hosted = True\n            marker_hits.update((tok for tok in t.platform_markers if tok in txt.lower()))\n        others.update((o for o in watch if o in txt.lower()))\n    readme_title, readme_head = ('', '')\n    readmes = sorted([p for p in paths if re.fullmatch('readme\\\\.(md|rst|txt)', p, re.I)], key=len) or sorted([p for p in paths if p.lower().endswith('/readme.md')], key=len)\n    if readmes:\n        head_lines = raw(readmes[0]).splitlines()[:60]\n        readme_head = '\\n'.join(head_lines)\n        for ln in head_lines:\n            if ln.strip().startswith('# '):\n                readme_title = ln.strip().lstrip('# ').strip()\n                break\n    rl = readme_head.lower()\n    domain_hits.update((d for d in t.platform_domains if d in rl))\n    marker_hits.update((m for m in t.platform_markers if m in rl))\n    win = event_window(event_date)\n    project_predates, tampered, earliest = (None, [], '')\n    if win:\n        st, cbody = gh(f\"https://api.github.com/repos/{owner}/{repo}/commits?until={win['start']}T00:00:00Z&per_page=1\")\n        if st == 200:\n            try:\n                lst = json.loads(cbody)\n                if isinstance(lst, list) and lst:\n                    c = lst[0].get('commit', {})\n                    dates = [(c.get('author') or {}).get('date', ''), (c.get('committer') or {}).get('date', '')]\n                    d = min((x for x in dates if x), default='')\n                    if d:\n                        project_predates = {'date': d, 'sha': (lst[0].get('sha') or '')[:7]}\n            except Exception:\n                project_predates = None\n        st, cbody = gh(f'https://api.github.com/repos/{owner}/{repo}/commits?per_page=100')\n        if st == 200:\n            try:\n                tampered, earliest = history_tamper_scan(json.loads(cbody), win)\n            except Exception:\n                tampered, earliest = ([], '')\n    if fetch_fails[0]:\n        return {'accessible': True, 'fetch_incomplete': True, 'note': f'{fetch_fails[0]} file fetch(es) failed — no verdict on partial retrieval'}\n    return {'accessible': True, 'file_count': len(paths), 'tech': detect_tech(paths, manifest_texts, hj_gh_languages(gh, owner, repo)), 'dependency': dependency, 'pipes': [], 'source_files': source_files, 'other_platforms': sorted(others), 'scaffold_only': False, 'sdk': {'callsites': callsites, 'file_spread': files_using, 'hosted': hosted, 'engine': api_used}, 'platform': {'domains': sorted(domain_hits), 'markers': sorted(marker_hits), 'files': artifact_files}, 'event_window': win, 'repo_created_at': created_at, 'history_penalty': history_penalty, 'project_predates': project_predates, 'history_tampered': tampered, 'earliest_commit': earliest, 'readme_title': readme_title, 'readme_head': readme_head, 'target_name': t.name}\n\ndef hj_evaluate_generic(evidence, t):\n    \"\"\"Deterministic scoring for an arbitrary target: dependency / invocation depth / runtime\n    API usage / env wiring / file spread / committed artifacts / deploy evidence, with the same\n    judge-set commit-history flags as the preset path.\"\"\"\n    Wt, TH = (t.weights, t.thresholds)\n    if not evidence.get('accessible'):\n        return {'tag': 'None', 'backbone': 'No', 'score': 0.0, 'pipelines': [], 'sdk': {}, 'breakdown': [], 'pipelines_called': 0, 'reason': 'inaccessible', 'scoring': 'generic'}\n    breakdown, acc = ([], [0.0])\n\n    def add(label, pts):\n        acc[0] = acc[0] + pts\n        breakdown.append({'signal': label, 'points': pts})\n    if evidence.get('dependency'):\n        add('dependency in manifest', Wt['dependency'])\n    sdk = evidence.get('sdk', {'callsites': 0, 'file_spread': 0, 'hosted': False, 'engine': False})\n    cs = sdk.get('callsites', 0)\n    if cs >= 3:\n        add(f'invocation call-sites ({cs})', Wt['invocation'])\n    if cs >= 8:\n        add('deep integration (8+ call-sites)', Wt['invocation_deep'])\n    if sdk.get('engine'):\n        add('runtime API usage', Wt['api_usage'])\n    if sdk.get('hosted'):\n        add('environment/key wiring', Wt['hosted'])\n    if sdk.get('file_spread', 0) >= 2:\n        add(f\"usage across {sdk['file_spread']} files\", Wt['file_spread'])\n    pf = evidence.get('platform') or {}\n    if pf.get('files'):\n        add(f\"target artifact committed: {pf['files'][0]}\", Wt['artifact'])\n    if pf.get('domains'):\n        add('deployed on target platform: ' + ', '.join(pf['domains'][:2]), Wt['platform_deploy'])\n    win = evidence.get('event_window')\n    project_predates = evidence.get('project_predates')\n    tampered = evidence.get('history_tampered') or []\n    pen = evidence.get('history_penalty')\n    pen = DEFAULT_HISTORY_PENALTY if pen is None else max(0.0, float(pen))\n    if win and project_predates:\n        pp_date = str(project_predates.get('date', ''))[:10]\n        early = str(evidence.get('earliest_commit', ''))[:10]\n        earliest = min((d for d in (pp_date, early) if d)) if pp_date or early else '?'\n        add(f\"⚠ FLAGGED: project history predates the event window {win['start']}..{win['end']} - work goes back to {earliest} (latest pre-window commit {project_predates.get('sha', '?')} on {pp_date or '?'}); judge-set penalty\", -pen)\n    if win and tampered:\n        t0 = tampered[0]\n        add(f\"⚠ FLAGGED: commit-date rewrite detected - {t0['sha']} authored {str(t0['author'])[:10]} but re-stamped {str(t0['committer'])[:10]} into the window; judge-set penalty\", -pen)\n    score = max(0.0, round(acc[0], 1))\n    real = cs > 0 or sdk.get('engine') or (bool(pf.get('domains')) and (bool(pf.get('files')) or bool(pf.get('markers')) or evidence.get('dependency')))\n    if not real:\n        backbone = 'No'\n    elif any((o in t.competitors for o in evidence.get('other_platforms', []))):\n        backbone = 'Partial'\n    else:\n        backbone = 'Yes'\n    if evidence.get('overclaim') or not evidence.get('accessible', True):\n        tag = 'None'\n    elif score >= TH['significant']:\n        tag = 'Significant' if backbone in ('Yes', 'Partial') else 'Moderate'\n    elif score >= TH['moderate']:\n        tag = 'Moderate'\n    elif score >= TH['less']:\n        tag = 'Less'\n    else:\n        tag = 'None'\n    if tag in ('None', 'Less'):\n        backbone = 'No'\n    return {'tag': tag, 'backbone': backbone, 'score': score, 'pipelines': [], 'sdk': sdk, 'breakdown': breakdown, 'pipelines_called': 0, 'pipelines_total': 0, 'other_platforms': evidence.get('other_platforms', []), 'tech': evidence.get('tech', []), 'platform': pf, 'target_name': t.name, 'scoring': 'generic', 'architecture': architecture_view(t, evidence), 'event_window': win, 'history_penalty': pen if win else None, 'project_predates': project_predates if win else None, 'history_tampered': tampered if win else [], 'earliest_commit': evidence.get('earliest_commit', ''), 'repo_created_at': evidence.get('repo_created_at', ''), 'reused_pipelines': []}\n\n\"\"\"Target-config extraction from a product vendor's own repo (\"Prefill from repo\").\n\nGiven the VENDOR's repository (not a consumer repo), derive a draft TargetConfig for the\nTargets editor: deterministic candidates from manifests / env examples / CLI definitions /\neditor manifests / docs, optionally synthesized by the LLM over the doc excerpts, then a\nVERIFICATION GATE drops any token that does not literally appear in the fetched corpus\n(same trust-but-verify philosophy as run_batch.apply_llm_mapping). Target-agnostic: no\nproduct names in code.\n\nSplit for the async endpoint:\n    prepare(url, gh, extra_url)  -> ctx dict  (sync, network via gh; run in a thread)\n    build_prompt(ctx)            -> str for ClassifierPool.ask (or None if no docs found)\n    finalize(ctx, answer_text)   -> {config, sources, warnings, suggestions}\nfinalize works with answer_text=\"\" too (deterministic-only fallback when the cloud is down).\n\"\"\"\nimport json\nimport re\nMAX_DOC_FILES = 6\nMAX_PKG_FILES = 12\nhj_ENV_RX = re.compile('\\\\b([A-Z][A-Z0-9]*_[A-Z0-9_]{2,})\\\\b')\nhj_URL_HOST_RX = re.compile('https?://([\\\\w-]+(?:\\\\.[\\\\w-]+)+)', re.I)\nhj_CMD_JS_RX = re.compile('\\\\.command\\\\(\\\\s*[\\'\\\\\"]([a-z][\\\\w:-]*)')\nhj_CMD_PY_RX = re.compile('add_parser\\\\(\\\\s*[\\'\\\\\"]([a-z][\\\\w-]*)')\nhj_GLOB_RX = re.compile('\"filenamePattern\"\\\\s*:\\\\s*\"(\\\\*[^\"]+)\"')\nhj_EXT_PROSE_RX = re.compile('`?\\\\*\\\\.([a-z][a-z0-9.]{1,11})`?')\nhj_DOTDIR_RX = re.compile('[\\'\\\\\"](\\\\.[a-z][\\\\w-]{2,24})/')\nhj_KEYPFX_RX = re.compile('startswith\\\\(\\\\s*[\\'\\\\\"]([a-z][a-z0-9]{1,11}_)[\\'\\\\\"]')\nhj_PYPROJ_NAME_RX = re.compile('^\\\\s*name\\\\s*=\\\\s*\"([^\"]+)\"', re.M)\nhj_PYPROJ_SCRIPT_RX = re.compile('^\\\\s*([\\\\w-]+)\\\\s*=\\\\s*\\\\\"[\\\\w.]+:[\\\\w]+\\\\\"', re.M)\nhj_GENERIC_ENV_PFX = {'NODE', 'NPM', 'PATH', 'HOME', 'PORT', 'HTTP', 'HTTPS', 'DEBUG', 'LOG', 'CI', 'GITHUB', 'GIT', 'AWS', 'GCP', 'GOOGLE', 'AZURE', 'DOCKER', 'K8S', 'OPENAI', 'ANTHROPIC', 'GEMINI', 'OLLAMA', 'PYTHON', 'PIP', 'VITE', 'REACT', 'NEXT', 'PUBLIC', 'DATABASE', 'REDIS', 'POSTGRES', 'API', 'APP', 'ENV', 'TEST', 'MAX', 'MIN', 'DEFAULT', 'CONST', 'PREFERENCE', 'VSCODE', 'EDITOR', 'THEME', 'COLOR', 'FONT', 'RENDER', 'WINDOW', 'PANEL'}\nhj_GENERIC_HOSTS = ('github.com', 'githubusercontent.com', 'npmjs.com', 'pypi.org', 'localhost', 'example.com', 'shields.io', 'img.shields', 'opensource.org', 'openai.com', 'anthropic.com', 'googleapis.com', 'google.com', 'microsoft.com', 'visualstudio.com', 'open-vsx.org', 'docker.com', 'kubernetes.io', 'amazonaws.com', 'w3.org', 'schema.org', 'readthedocs')\nhj_DOC_HOST_PFX = ('docs.', 'www.', 'blog.', 'status.', 'doc.', 'readthedocs.')\nhj_SKIP_PKG_RX = re.compile('(?:^|/)(?:comparative[-_])?(?:starter(?:-kit)?|example|examples|demo|evals?|benchmark|playground|frontend|[-_]mcp)(?:$|[-_/])|-(?:starter(?:-kit)?|example|examples|demo|evals?|mcp|frontend)$', re.I)\nhj_EDITOR_PATH_RX = re.compile('(^|/)([^/]*(frontend|vscode|\\\\.vscode|editor|webui)[^/]*)(/|$)', re.I)\nhj_GENERIC_CMDS = {'brew', 'cargo', 'npm', 'npx', 'pip', 'pip3', 'cd', 'echo', 'export', 'curl', 'wget', 'git', 'sudo', 'mkdir', 'cat', 'ls', 'docker', 'make', 'python', 'python3', 'node', 'yarn', 'pnpm', 'uv', 'apt', 'apk', 'choco', 'scoop', 'winget', 'source', 'set'}\nhj_NAME_SKIP = {'quick start', 'getting started', 'home', 'documentation', 'docs', 'readme', 'install', 'overview', 'index', 'tier 1', 'tier 2', 'tier 3', 'pricing', 'license', 'contributing', 'changelog', 'security', 'code of conduct'}\nhj_STOP_WORDS = {'the', 'a', 'an', 'to', 'of', 'in', 'for', 'with', 'and', 'or', 'on', 'as', 'by', 'from', 'this', 'that', 'is', 'are', 'be', 'it', 'we', 'you', 'your', 'our', 'learn', 'build', 'see', 'graph', 'use', 'using', 'turns', 'documents', 'into', 'text', 'memory', 'about', 'pipeline'}\nhj_CLI_PROSE = hj_STOP_WORDS | {'learn', 'build', 'see', 'graph', 'using', 'turns', 'documents'}\nhj_NOT_METHODS = {'ai', 'io', 'com', 'org', 'dev', 'app', 'net', 'svg', 'png', 'jpg', 'jpeg', 'gif', 'ico', 'css', 'js', 'ts', 'md', 'json', 'html', 'htm', 'toml', 'lock', 'map', 'py', 'rs', 'go', 'txt', 'yml', 'yaml', 'pdf', 'xml', 'svgx', 'woff', 'ttf'}\nhj_CONSUMER_ENV_RX = re.compile('(API_KEY|API_URL|BASE_URL|BACKEND_URL|ENDPOINT|_TOKEN$|_SECRET$|_HOST$|CLOUD)', re.I)\nhj_FENCE_RX = re.compile('```(?:bash|shell|sh|zsh|console|powershell)\\\\b[^\\\\n]*\\\\n(.*?)```', re.I | re.S)\nhj_TITLE_RX = re.compile('<title>([^<]+)</title>', re.I)\nhj_STRONG_RX = re.compile('<strong>([^<]{2,48})</strong>', re.I)\nhj_H1_RX = re.compile('^#\\\\s+(.+)$', re.M)\nhj_IMPORT_RX = re.compile('(?m)^\\\\s*(?:import\\\\s+([a-z][\\\\w.]*)|from\\\\s+([a-z][\\\\w.]*)\\\\s+import)', re.I)\nhj_CARGO_PKG_RX = re.compile('(?ms)\\\\[package\\\\].*?^name\\\\s*=\\\\s*\\\\\"([^\\\\\"]+)\\\\\"')\nhj_CARGO_BIN_RX = re.compile('(?ms)\\\\[\\\\[bin\\\\]\\\\].*?^name\\\\s*=\\\\s*\\\\\"([^\\\\\"]+)\\\\\"')\nhj_CMD_RS_RX = re.compile('#\\\\[(?:command|clap)\\\\([^)]*name\\\\s*=\\\\s*\"([a-z][\\\\w-]*)\"', re.I)\nhj_GENERIC_EXTS = {'js', 'ts', 'tsx', 'jsx', 'py', 'md', 'json', 'txt', 'yml', 'yaml', 'css', 'html', 'sh', 'env', 'lock', 'toml', 'cfg', 'ini', 'xml', 'png', 'svg', 'jpg', 'csv', 'xlsx', 'pdf', 'map', 'cjs', 'mjs', 'log', 'zip', 'gz', 'tgz', 'vsix', 'whl', 'tar', 'exe', 'dmg', 'jar', 'iso', 'deb', 'rpm'}\nhj_GENERIC_DOTDIRS = {'.github', '.vscode', '.git', '.idea', '.venv', '.env', '.cache', '.config', '.local', '.docker', '.devcontainer', '.husky', '.next', '.expo', '.vite', '.pytest_cache', '.tox', '.mypy_cache', '.circleci'}\n\ndef extract_infer_architecture_template(types):\n    \"\"\"Same product-class map as the TypeScript `inferArchitectureTemplate` (types-only).\"\"\"\n    tset = {str(x).lower() for x in types or []}\n    if 'platform' in tset and ('code' in tset or 'api' in tset):\n        return 'data_platform'\n    if 'platform' in tset:\n        return 'deploy'\n    if 'api' in tset and 'code' not in tset:\n        return 'api'\n    return 'sdk'\n\ndef hj_name_from_title(title):\n    \"\"\"Turn 'Quick Start - Hotdata Docs' / 'Hotdata CLI' into a product display name.\"\"\"\n    title = re.sub('\\\\s+', ' ', title or '').strip()\n    if not title:\n        return ''\n    if re.fullmatch('tier\\\\s*\\\\d+', title, flags=re.I):\n        return ''\n    parts = [p.strip() for p in re.split('\\\\s+[-|–—]\\\\s+', title) if p.strip()]\n    for part in reversed(parts or [title]):\n        cleaned = re.sub('\\\\bdocs?\\\\b', '', part, flags=re.I)\n        cleaned = re.sub('\\\\s+CLI\\\\s*$', '', cleaned, flags=re.I).strip(' -')\n        if cleaned and cleaned.lower() not in hj_NAME_SKIP and (1 < len(cleaned) <= 40):\n            if re.fullmatch('tier\\\\s*\\\\d+', cleaned, flags=re.I):\n                continue\n            return cleaned\n    return ''\n\ndef hj_product_stems(cand, ctx=None):\n    ctx = ctx or {}\n    stems = set()\n    for n in (cand.get('dependency_names') or []) + (cand.get('cli_bins') or []):\n        s = re.sub('[^a-z0-9]', '', re.sub('-cli$', '', (n or '').lower()))\n        if len(s) >= 3:\n            stems.add(s)\n    repo = str(ctx.get('repo') or '').split('/')[-1]\n    rs = re.sub('[^a-z0-9]', '', re.sub('-cli$', '', repo.lower()))\n    if len(rs) >= 3:\n        stems.add(rs)\n    return stems\n\ndef hj_plausible_name(name, stems):\n    n = (name or '').strip()\n    if not n or n.lower() in hj_NAME_SKIP or re.fullmatch('tier\\\\s*\\\\d+', n, flags=re.I):\n        return False\n    if not re.search('[A-Za-z]', n):\n        return False\n    nl = re.sub('[^a-z0-9]', '', n.lower())\n    if len(nl) < 3:\n        return False\n    if not stems:\n        return True\n    return any((s in nl or nl in s for s in stems))\n\ndef hj_ok_invocation(token, stems):\n    t = (token or '').strip()\n    if re.match('^(import|from)\\\\s+[a-z]', t, re.I):\n        return True\n    m = re.fullmatch('([a-z][\\\\w-]*)\\\\.([a-z][\\\\w]*)', t, re.I)\n    if m:\n        meth = m.group(2).lower()\n        if meth in hj_NOT_METHODS or meth in hj_GENERIC_CMDS:\n            return False\n        stem = re.sub('[^a-z0-9]', '', m.group(1).lower())\n        return not stems or stem in stems\n    return hj_is_cli_phrase(t)\n\ndef hj_is_cli_phrase(phrase, known=None):\n    parts = [p for p in re.split('\\\\s+', (phrase or '').strip()) if p]\n    if not parts:\n        return False\n    if any((not re.fullmatch('[A-Za-z0-9_./\\\\-]+', p) for p in parts)):\n        return False\n    if '\"' in phrase or \"'\" in phrase:\n        return False\n    if re.search('\\\\.(svg|png|jpg|jpeg|gif|ai|io|com|org|dev|net|html?|md)$', parts[0], re.I):\n        return False\n    bin_name = re.sub('[^a-z0-9-]', '', parts[0].lower())\n    if bin_name in hj_GENERIC_CMDS or bin_name in hj_STOP_WORDS:\n        return False\n    if any((re.sub('[^a-z0-9-]', '', p.lower()) in hj_CLI_PROSE for p in parts[1:])):\n        return False\n    if known is not None and bin_name not in known:\n        return False\n    return True\n\ndef hj_pkg_alias(primary, name):\n    \"\"\"Same published product (scoped npm / -cli / -sdk), not a sibling workspace package.\"\"\"\n    p = re.sub('[^a-z0-9]', '', (primary or '').rsplit('/', 1)[-1].lower())\n    n = re.sub('[^a-z0-9]', '', (name or '').rsplit('/', 1)[-1].lower())\n    if not p or not n:\n        return False\n    if n == p:\n        return True\n    extra = n[len(p):] if n.startswith(p) else ''\n    return extra in ('ts', 'js', 'py', 'cli', 'sdk')\n\ndef hj_consumer_pkg_name(name):\n    \"\"\"True if this looks like a package a consumer installs, not a workspace extra.\"\"\"\n    n = (name or '').strip()\n    if not n:\n        return False\n    stem = n.rsplit('/', 1)[-1]\n    if hj_SKIP_PKG_RX.search(stem) or hj_SKIP_PKG_RX.search(n):\n        return False\n    return True\n\ndef hj_is_platform_host(host):\n    hl = (host or '').lower().split('/')[0]\n    if not hl or any((hl.startswith(p) for p in hj_DOC_HOST_PFX)):\n        return False\n    return bool(re.search('(^|\\\\.)(app|cloud|dashboard|platform)(\\\\.|$)', hl) or 'cloud' in hl)\n\ndef hj_commands_from_text(text, whole_is_code=False):\n    \"\"\"CLI-like phrases from fenced / pre samples — not from prose sentences.\"\"\"\n    chunks = []\n    if whole_is_code:\n        chunks.append(text or '')\n    else:\n        chunks.extend(hj_FENCE_RX.findall(text or ''))\n    out = []\n    for chunk in chunks:\n        for raw in (chunk or '').splitlines():\n            line = re.sub('^\\\\s*(?:[$#]\\\\s*|PS>\\\\s*)', '', raw).strip()\n            if not line or line.startswith('#'):\n                continue\n            phrase = ' '.join(line.split()[:4])\n            if not hj_is_cli_phrase(phrase):\n                continue\n            out.append(phrase)\n    return list(dict.fromkeys(out))\n\ndef hj_imports_from_text(text):\n    out = []\n    for m in hj_IMPORT_RX.finditer(text or ''):\n        tok = next((g for g in m.groups() if g), '')\n        tok = tok.split('::', 1)[0].split('.', 1)[0]\n        if tok and tok.lower() not in hj_GENERIC_CMDS:\n            out.append(tok)\n    return list(dict.fromkeys(out))\n\ndef hj_package_title(cand, ctx=None):\n    names = cand.get('dependency_names') or []\n    if names:\n        n = re.sub('-cli$', '', names[0], flags=re.I).replace('-', ' ').strip()\n        return n.title() if n else ''\n    repo = str((ctx or {}).get('repo') or '').split('/')[-1]\n    repo = re.sub('-cli$', '', repo, flags=re.I).replace('-', ' ').strip()\n    return repo.title() if repo else ''\n\ndef hj_display_name(cand, ctx=None):\n    ctx = ctx or {}\n    stems = hj_product_stems(cand, ctx)\n    ranked = []\n\n    def consider(name, rank):\n        n = (name or '').strip()\n        if hj_plausible_name(n, stems):\n            ranked.append((rank, n))\n    for path, txt in (ctx.get('corpus') or {}).items():\n        docsish = path.startswith('docs:') or path.lower().endswith(('.md', '.rst', '.html', '.txt'))\n        hm = hj_H1_RX.search(txt or '')\n        if hm:\n            consider(hj_name_from_title(re.sub('</?[^>]+>', '', hm.group(1))), 0)\n        if path.startswith('docs:') or '<title' in (txt or '')[:2000].lower():\n            tm = hj_TITLE_RX.search(txt or '')\n            if tm:\n                consider(hj_name_from_title(tm.group(1)), 1)\n        first = (txt or '').strip().splitlines()[:1]\n        if docsish and first and ('<' not in first[0]):\n            consider(hj_name_from_title(first[0]), 2)\n        for sm in hj_STRONG_RX.finditer(txt or ''):\n            consider(hj_name_from_title(sm.group(1)), 3)\n    if ranked:\n        ranked.sort(key=lambda kv: kv[0])\n        return ranked[0][1]\n    desc = str(ctx.get('github_description') or '')\n    m = re.search('\\\\b(?:for|CLI(?:\\\\s+tool)?\\\\s+for)\\\\s+([A-Z][A-Za-z0-9.]+)', desc)\n    if m and hj_plausible_name(m.group(1).rstrip('.'), stems):\n        return m.group(1).rstrip('.')\n    return hj_package_title(cand, ctx)\n\ndef hj_fetch(url, gh):\n    \"\"\"(owner, repo, paths, raw_fn) for a repo, or None if unreadable.\"\"\"\n    pr = parse_repo(url)\n    if not pr:\n        return None\n    owner, repo = pr\n    st, body = gh(f'https://api.github.com/repos/{owner}/{repo}')\n    if st != 200:\n        return None\n    meta = json.loads(body)\n    branch = meta.get('default_branch', 'main')\n    description = str(meta.get('description') or '')\n    st, tbody = gh(f'https://api.github.com/repos/{owner}/{repo}/git/trees/{branch}?recursive=1')\n    if st != 200:\n        return None\n    paths = [x.get('path', '') for x in json.loads(tbody).get('tree', [])]\n\n    def raw(p):\n        return gh(f'https://raw.githubusercontent.com/{owner}/{repo}/{branch}/{p}')[1]\n    return (owner, repo, paths, raw, description)\n\ndef hj_interesting_files(paths):\n    \"\"\"Pick the small set of files worth fetching, bucketed by role. Candidates are\n    collected first and then RANKED per bucket - a monorepo has dozens of plausible\n    matches, and depth alone picks the wrong ones (e.g. app scripts over the real CLI).\"\"\"\n    buckets = {'pkg': [], 'env': [], 'cli': [], 'docs': [], 'consts': []}\n    for p in paths:\n        pl = p.lower()\n        if 'node_modules/' in pl or '/dist/' in pl or '/build/' in pl or ('test' in pl):\n            continue\n        if any((hj_SKIP_PKG_RX.search(seg) or hj_EDITOR_PATH_RX.search(seg) for seg in pl.split('/'))):\n            continue\n        base = pl.rsplit('/', 1)[-1]\n        depth = p.count('/')\n        if base in ('package.json', 'pyproject.toml', 'setup.py', 'cargo.toml', 'go.mod'):\n            prio = 0 if depth == 0 else 1 if re.search('(^|/)(packages?|clients?|sdk|libs?|core)(/|$)', pl) else 2\n            buckets['pkg'].append((prio, depth, p))\n        elif base.endswith(('.env.example', '.env.sample', '.env.template', 'env.example')):\n            buckets['env'].append((depth, p))\n        elif pl.endswith(('.py', '.ts', '.js', '.rs', '.go')) and re.search('(^|/)cli/|/cli\\\\.|command', pl):\n            buckets['cli'].append((0 if '/cli/' in pl or pl.endswith(('cli.py', 'cli.rs', 'cli.go')) else 1, depth, p))\n        elif re.search('constants\\\\.(ts|js|py)$|agent-manager\\\\.ts$', pl):\n            if hj_EDITOR_PATH_RX.search(pl):\n                continue\n            buckets['consts'].append((0 if 'client' in pl or 'sdk' in pl else 1, depth, p))\n        elif base.endswith(('.md', '.rst', '.txt')) and re.search('readme|quickstart|getting.?started', base):\n            buckets['docs'].append((0 if 'quickstart' in base else 1, depth, p))\n    caps = {'pkg': MAX_PKG_FILES, 'env': 4, 'cli': 10, 'docs': MAX_DOC_FILES, 'consts': 5}\n    return {k: [t[-1] for t in sorted(v)[:caps[k]]] for k, v in buckets.items()}\n\ndef hj_candidates(corpus, pick):\n    \"\"\"Deterministic candidates + per-field sources from the fetched corpus.\"\"\"\n    cand = {'dependency_names': [], 'cli_bins': [], 'cli_bins_raw': [], 'cli_verbs': [], 'cli_cmds': [], 'invocations': [], 'env_vars': [], 'hosts': [], 'artifacts': [], 'dotdirs': [], 'key_prefixes': []}\n    src = {}\n\n    def note(field, path):\n        src.setdefault(field, set()).add(path)\n    for p in pick['pkg']:\n        txt = corpus.get(p, '')\n        pl = p.lower()\n        if pl.endswith('package.json'):\n            try:\n                d = json.loads(txt)\n            except Exception:\n                continue\n            name = d.get('name', '')\n            if name and d.get('private') is not True and hj_consumer_pkg_name(name):\n                cand['dependency_names'].append(name)\n                note('dependency_names', p)\n            for b in d.get('bin') or {}:\n                cand['cli_bins'].append(b)\n                cand['cli_bins_raw'].append(b)\n                note('cli_verbs', p)\n            for sel in d.get('contributes', {}).get('customEditors') or []:\n                for s in sel.get('selector') or []:\n                    g = s.get('filenamePattern', '')\n                    if g.startswith('*.'):\n                        cand['artifacts'].append(g)\n                        note('artifacts', p)\n        elif pl.endswith('cargo.toml'):\n            pkg = hj_CARGO_PKG_RX.search(txt)\n            if pkg and hj_consumer_pkg_name(pkg.group(1)):\n                cand['dependency_names'].append(pkg.group(1))\n                note('dependency_names', p)\n            for bm in hj_CARGO_BIN_RX.finditer(txt):\n                cand['cli_bins'].append(bm.group(1))\n                cand['cli_bins_raw'].append(bm.group(1))\n                note('cli_verbs', p)\n        else:\n            m = hj_PYPROJ_NAME_RX.search(txt)\n            if m and hj_consumer_pkg_name(m.group(1)):\n                cand['dependency_names'].append(m.group(1))\n                note('dependency_names', p)\n            if '[project.scripts]' in txt:\n                seg = txt.split('[project.scripts]', 1)[1][:600]\n                for sm in hj_PYPROJ_SCRIPT_RX.finditer(seg):\n                    cand['cli_bins'].append(sm.group(1))\n                    cand['cli_bins_raw'].append(sm.group(1))\n                    note('cli_verbs', p)\n    env_paths = pick['env'] or []\n    if env_paths:\n        for p in env_paths:\n            for v in hj_ENV_RX.findall(corpus.get(p, '')):\n                cand['env_vars'].append(v)\n                note('hosted_markers', p)\n    else:\n        for p in pick['docs']:\n            for m in re.finditer('\\\\b([A-Z][A-Z0-9]*_[A-Z0-9_]{2,})\\\\s*=', corpus.get(p, '')):\n                cand['env_vars'].append(m.group(1))\n                note('hosted_markers', p)\n    for p, txt in corpus.items():\n        for h in hj_URL_HOST_RX.findall(txt):\n            hl = h.lower()\n            if not any((g in hl for g in hj_GENERIC_HOSTS)):\n                cand['hosts'].append(hl)\n                note('hosted_markers', p)\n        for k in hj_KEYPFX_RX.findall(txt):\n            cand['key_prefixes'].append(k)\n            note('platform_markers', p)\n    for p in pick['env'] + pick['pkg']:\n        for d in hj_DOTDIR_RX.findall(corpus.get(p, '')):\n            if d.lower() not in hj_GENERIC_DOTDIRS:\n                cand['dotdirs'].append(d)\n                note('platform_files', p)\n    for p, txt in corpus.items():\n        for v in hj_CMD_JS_RX.findall(txt) + hj_CMD_PY_RX.findall(txt) + hj_CMD_RS_RX.findall(txt):\n            cand['cli_verbs'].append(v)\n            note('cli_verbs', p)\n        docsish = p.startswith('docs:') or p.startswith('upload:') or p.lower().endswith(('.md', '.rst', '.txt', '.html'))\n        if docsish:\n            known = {re.sub('[^a-z0-9-]', '', x.lower()) for x in cand['cli_bins'] + cand['cli_bins_raw'] if x}\n            known = {k for k in known if len(k) >= 3 and k not in hj_STOP_WORDS} or None\n            dep_stems = {re.sub('[^a-z0-9-]', '', x.lower()) for x in cand['dependency_names'] if x}\n            dep_stems = {k for k in dep_stems if len(k) >= 3}\n            for phrase in hj_commands_from_text(txt, whole_is_code='#code' in p):\n                if not hj_is_cli_phrase(phrase, known):\n                    continue\n                cand['cli_cmds'].append(phrase)\n                bin_name = phrase.split()[0]\n                existing = {b.lower() for b in cand['cli_bins']}\n                if bin_name.lower() in existing or '-' in bin_name:\n                    cand['cli_bins_raw'].append(bin_name)\n                    cand['cli_bins'].append(bin_name)\n                note('cli_verbs', p)\n            for imp in hj_imports_from_text(txt):\n                if dep_stems and re.sub('[^a-z0-9-]', '', imp.lower()) not in dep_stems:\n                    continue\n                cand.setdefault('invocations', []).append(f'import {imp}' if ' ' not in imp else imp)\n                note('invocation', p)\n            for dep in cand['dependency_names'][:3]:\n                stem = re.sub('[^a-z0-9-]', '', (dep or '').lower())\n                if len(stem) < 3:\n                    continue\n                for meth in re.findall(f'\\\\b{re.escape(stem)}\\\\.([a-z][\\\\w]*)\\\\s*\\\\(', txt or '', re.I):\n                    if meth.lower() in hj_GENERIC_CMDS or meth.lower() in hj_NOT_METHODS:\n                        continue\n                    cand.setdefault('invocations', []).append(f'{stem}.{meth}')\n                    note('invocation', p)\n    for p in pick['docs']:\n        for e in hj_EXT_PROSE_RX.findall(corpus.get(p, '')):\n            if e.rstrip('.') not in hj_GENERIC_EXTS:\n                cand['artifacts'].append(f'*.{e}')\n                note('artifacts', p)\n    all_lower = '\\n'.join(corpus.values()).lower()\n    counts = {}\n    for v in cand['env_vars']:\n        pfx = v.split('_', 1)[0]\n        if pfx in hj_GENERIC_ENV_PFX or len(pfx) < 3:\n            continue\n        if f'{v.lower()}.md' in all_lower and f'{v.lower()}=' not in all_lower:\n            continue\n        counts.setdefault(pfx, set()).add(v)\n    cluster = max(counts.items(), key=lambda kv: len(kv[1]))[1] if counts else set()\n    product_pfx = {re.split('[^A-Z0-9]+', n.upper())[0] for n in cand['dependency_names'] + cand['cli_bins'] if n}\n    product_pfx = {p for p in product_pfx if len(p) >= 4}\n    if product_pfx and counts:\n        matched = {pfx: vs for pfx, vs in counts.items() if pfx in product_pfx or any((pfx.startswith(p[:4]) or p.startswith(pfx) for p in product_pfx))}\n        if matched:\n            cluster = max(matched.items(), key=lambda kv: len(kv[1]))[1]\n    consumer = {v for v in cluster if hj_CONSUMER_ENV_RX.search(v)}\n    cand['env_vars'] = sorted(consumer or cluster)[:8]\n    from collections import Counter\n    own_tokens = {re.sub('[^a-z0-9]', '', t.lower()) for t in cand['dependency_names'] + cand['cli_bins'] if len(t) >= 4}\n    freq = Counter(cand['hosts'])\n    cand['hosts'] = [h for h, _ in sorted(freq.items(), key=lambda kv: (0 if any((t and t in kv[0].replace('.', '').replace('-', '') for t in own_tokens)) else 1, -kv[1]))][:8]\n    for k in cand:\n        cand[k] = list(dict.fromkeys(cand[k]))\n    cand['dependency_names'] = sorted(cand['dependency_names'], key=lambda n: (n.rsplit('/', 1)[-1].count('-'), len(n.rsplit('/', 1)[-1]), n))\n    if cand['dependency_names']:\n        primary = cand['dependency_names'][0]\n        cand['dependency_names'] = [n for n in cand['dependency_names'] if hj_pkg_alias(primary, n)]\n    return (cand, {f: sorted(ps) for f, ps in src.items()})\n\ndef hj_http_get(url, timeout=45):\n    \"\"\"Plain HTTP fetch for docs pages / registries (never sends the GitHub token).\"\"\"\n    try:\n        req = hj_rq.Request(url, headers={'User-Agent': 'Mozilla/5.0 (compatible; HackJudge-TargetExtract/1.0)', 'Accept': 'text/html,application/json;q=0.9,*/*;q=0.8'})\n        with hj_rq.urlopen(req, timeout=timeout) as r:\n            return (r.status, r.read(1000000).decode('utf-8', 'replace'))\n    except Exception as exc:\n        return (0, str(exc))\nhj_TAG_RX = re.compile('<script[\\\\s\\\\S]*?</script>|<style[\\\\s\\\\S]*?</style>|<[^>]+>')\nhj_HREF_RX = re.compile('href=[\"\\\\\\']([^\"\\\\\\'#?]+)')\n\ndef hj_html_text(body):\n    return re.sub('[ \\\\t]+', ' ', hj_html.unescape(hj_TAG_RX.sub(' ', body)))\n\ndef hj_docs_corpus(url):\n    \"\"\"The given docs page + up to 3 same-host quickstart-ish linked pages, as text.\"\"\"\n    corpus = {}\n    st, body = hj_http_get(url)\n    if st != 200 or not body or st == 0:\n        detail = f' ({body[:160]})' if body and st != 200 else ''\n        return (corpus, [f'Docs URL could not be fetched: {url}{detail}'])\n    corpus[f'docs:{url}'] = hj_html_text(body) if '<' in body[:500] else body\n    host = urlparse(url).netloc\n    seen = set()\n    for href in hj_HREF_RX.findall(body):\n        if len(seen) >= 3:\n            break\n        full = urljoin(url, href)\n        if urlparse(full).netloc != host or full == url or full in seen or (not re.search('quickstart|getting|install|setup|cli|environment', full, re.I)):\n            continue\n        seen.add(full)\n        st2, b2 = hj_http_get(full)\n        if st2 == 200 and b2:\n            corpus[f'docs:{full}'] = hj_html_text(b2) if '<' in b2[:500] else b2\n    return (corpus, [])\n\ndef hj_registry_corpus(pkg):\n    \"\"\"npm + PyPI metadata for a package name - public even when the repo is private.\n    Emitted as synthetic manifest/README corpus entries so the normal parsers apply.\"\"\"\n    corpus, warns, found = ({}, [], False)\n    st, body = hj_http_get(f\"https://registry.npmjs.org/{pkg.replace('/', '%2F')}\")\n    if st == 200:\n        try:\n            d = json.loads(body)\n            latest = (d.get('dist-tags') or {}).get('latest', '')\n            v = (d.get('versions') or {}).get(latest, {})\n            man = {'name': d.get('name', pkg)}\n            if v.get('bin'):\n                man['bin'] = v['bin']\n            corpus[f'npm:{pkg}/package.json'] = json.dumps(man)\n            if d.get('readme'):\n                corpus[f'npm:{pkg}/README.md'] = str(d['readme'])[:60000]\n            found = True\n        except Exception:\n            pass\n    st, body = hj_http_get(f'https://pypi.org/pypi/{pkg}/json')\n    if st == 200:\n        try:\n            info = json.loads(body).get('info', {})\n            corpus[f'pypi:{pkg}/pyproject.toml'] = f'''name = \"{info.get('name', pkg)}\"'''\n            if info.get('description'):\n                corpus[f'pypi:{pkg}/README.md'] = str(info['description'])[:60000]\n            found = True\n        except Exception:\n            pass\n    if not found:\n        warns.append(f'Package not found on npm or PyPI: {pkg}')\n    return (corpus, warns)\n\ndef hj_bucket_for_upload(name):\n    nl = name.lower()\n    if nl.endswith(('package.json', 'pyproject.toml', 'setup.py')):\n        return 'pkg'\n    if '.env' in nl or nl.endswith(('.example', '.sample')):\n        return 'env'\n    if nl.endswith(('.ts', '.js', '.py')):\n        return 'consts'\n    return 'docs'\n\ndef prepare_sources(gh, repo_url=None, extra_url=None, docs_url=None, package=None, uploads=None):\n    \"\"\"Build one corpus from any mix of sources (vendor repo, docs site, registry\n    package, uploaded files), then run the same candidate extraction. Returns None only\n    if NO source yielded content.\"\"\"\n    corpus = {}\n    pick = {'pkg': [], 'env': [], 'cli': [], 'docs': [], 'consts': []}\n    warnings = []\n    label_parts = []\n    github_description = ''\n    if repo_url:\n        fetched = hj_fetch(repo_url, gh)\n        if fetched:\n            owner, repo, paths, raw = (fetched[0], fetched[1], fetched[2], fetched[3])\n            github_description = fetched[4] if len(fetched) > 4 else ''\n            rpick = hj_interesting_files(paths)\n            for grp, ps in rpick.items():\n                for p in ps:\n                    corpus[p] = raw(p)\n                    pick[grp].append(p)\n            label_parts.append(f'{owner}/{repo}')\n        else:\n            warnings.append(f'Repo could not be read: {repo_url}')\n    if extra_url:\n        extra = hj_fetch(extra_url, gh)\n        if extra:\n            epaths, eraw = (extra[2], extra[3])\n            epick = hj_interesting_files(epaths)\n            for grp, ps in epick.items():\n                for p in ps:\n                    corpus[f'extra:{p}'] = eraw(p)\n                    pick[grp].append(f'extra:{p}')\n        else:\n            warnings.append(f'Second repo could not be read: {extra_url}')\n    if docs_url:\n        dcorp, dwarns = hj_docs_corpus(docs_url)\n        corpus.update(dcorp)\n        pick['docs'].extend(dcorp.keys())\n        warnings.extend(dwarns)\n        if dcorp:\n            label_parts.append(urlparse(docs_url).netloc)\n    if package:\n        rcorp, rwarns = hj_registry_corpus(package.strip())\n        corpus.update(rcorp)\n        for k in rcorp:\n            pick['pkg' if k.endswith(('.json', '.toml')) else 'docs'].append(k)\n        warnings.extend(rwarns)\n        if rcorp:\n            label_parts.append(package.strip())\n    for name, text in (uploads or {}).items():\n        key = f'upload:{name}'\n        corpus[key] = text\n        pick[hj_bucket_for_upload(name)].append(key)\n    if uploads:\n        label_parts.append(f'{len(uploads)} uploaded file(s)')\n    if not corpus:\n        return None\n    cand, sources = hj_candidates(corpus, pick)\n    if not (docs_url and any((k.startswith('docs:') for k in corpus))) and cand.get('dependency_names'):\n        primary = cand['dependency_names'][0]\n        if not package or package.strip().lower() != primary.lower():\n            rcorp, rwarns = hj_registry_corpus(primary)\n            if rcorp:\n                corpus.update(rcorp)\n                for k in rcorp:\n                    pick['pkg' if k.endswith(('.json', '.toml')) else 'docs'].append(k)\n                cand, sources = hj_candidates(corpus, pick)\n            warnings.extend(rwarns)\n    docs_excerpts = '\\n\\n'.join((f'--- {p} ---\\n{corpus[p][:2500]}' for p in pick['docs'][:5])) or ''\n    return {'repo': ' + '.join(label_parts) or 'provided sources', 'candidates': cand, 'sources': sources, 'corpus': corpus, 'corpus_lower': '\\n'.join(corpus.values()).lower(), 'docs_excerpts': docs_excerpts[:12000], 'warnings': warnings, 'github_description': github_description}\n\ndef prepare(url, gh, extra_url=None):\n    \"\"\"Back-compat wrapper: repo-only extraction.\"\"\"\n    return prepare_sources(gh, repo_url=url, extra_url=extra_url)\nhj_PROMPT = 'You are configuring a hackathon-judging tool. From the VENDOR repo material below,\\nproduce the detection signals a judge would use to verify that a CONSUMER project genuinely\\nused this product. Return ONLY a strict JSON object (start with { and end with }):\\n{\\n  \"name\": \"<product name>\",\\n  \"dependency_names\": \"<comma-separated package names a consumer installs>\",\\n  \"artifacts\": \"<comma-separated file globs/conventions consumers commit, e.g. *.pipe>\",\\n  \"invocation\": \"<pipe-separated code patterns consumers write: client classes, key methods, import forms>\",\\n  \"hosted_markers\": \"<comma-separated consumer-side env var names and API hosts>\",\\n  \"cli_verbs\": \"<comma-separated CLI commands incl. binary name, e.g. \\'acme start\\'>\",\\n  \"platform_domains\": \"<comma-separated vendor platform/deploy domains>\",\\n  \"platform_files\": \"<comma-separated config files/dirs the product writes into consumer repos>\",\\n  \"platform_markers\": \"<comma-separated key/id prefixes, e.g. sk_>\",\\n  \"suggestions\": { \"competitors\": \"<comma-separated, MAY use general knowledge>\",\\n                    \"neutral\": \"<comma-separated commonly co-used neutral tools>\" }\\n}\\nRules: every value OUTSIDE \"suggestions\" must be evidenced by the material below - do not\\ninvent. Prefer consumer-side signals over vendor-internal ones (internal env prefixes, private\\nworkspace packages, ops domains are NOT detection signals). NEVER derive competitors from the\\nrepo itself - a product\\'s integrations are not its rivals; competitors go ONLY in suggestions.\\nLeave a field as \"\" if the material shows nothing for it. No em dashes anywhere.\\n\\nDETERMINISTIC CANDIDATES (pre-extracted, with the same evidence rules):\\n%%CANDIDATES%%\\n\\nDOC EXCERPTS:\\n%%DOCS%%'\n\ndef build_prompt(ctx):\n    return hj_PROMPT.replace('%%CANDIDATES%%', json.dumps(ctx['candidates'], indent=1)).replace('%%DOCS%%', ctx['docs_excerpts'] or '(no docs found)')\n\ndef hj_verify_field(value, corpus_lower, sep):\n    \"\"\"Keep only tokens evidenced in the fetched corpus. Verification is per WORD: a\n    multi-word token like \"rocketride start\" passes when both words appear (the binary\n    and its verb live in different files), but a token with any un-evidenced word drops.\"\"\"\n    kept, dropped = ([], [])\n    for tok in re.split('[|,\\\\n]', value or ''):\n        t = tok.strip()\n        if not t:\n            continue\n        words = [w.lstrip('*.').lower() for w in re.split('\\\\s+', t)]\n        words = [w for w in words if w]\n        if words and all((w in hj_STOP_WORDS for w in words)):\n            dropped.append(t)\n            continue\n        ok = bool(words) and all((w in corpus_lower for w in words))\n        (kept if ok else dropped).append(t)\n    return (sep.join(dict.fromkeys(kept)), dropped)\n\ndef hj_fallback_config(cand, ctx=None):\n    \"\"\"Deterministic-only draft (used when the LLM is unreachable or returns junk).\"\"\"\n    from collections import Counter\n    ctx = ctx or {}\n    ranked = sorted(Counter((b for b in cand['cli_bins_raw'] if b.lower() not in hj_STOP_WORDS and b.lower() not in hj_GENERIC_CMDS)).items(), key=lambda kv: (-kv[1], len(kv[0])))\n    bins = [b for b, _ in ranked] or [b for b in cand['cli_bins'] if b.lower() not in hj_STOP_WORDS]\n    known = {re.sub('[^a-z0-9-]', '', x.lower()) for x in cand['cli_bins'] + cand['cli_bins_raw'] if x}\n    known = {k for k in known if len(k) >= 3 and k not in hj_STOP_WORDS} or None\n    cmds = [c for c in dict.fromkeys(cand.get('cli_cmds') or []) if hj_is_cli_phrase(c, known)]\n    verbs = cmds[:8] or ([f'{bins[0]} {v}' for v in cand['cli_verbs'][:6] if v.lower() not in hj_STOP_WORDS] if bins else [])\n    verbs = [v for v in verbs if hj_is_cli_phrase(v, known) or (bins and v.split()[0].lower() == bins[0].lower() and hj_is_cli_phrase(v))]\n    stems = hj_product_stems(cand, ctx)\n    inv = [t for t in dict.fromkeys(cand.get('invocations') or []) if hj_ok_invocation(t, stems)][:8]\n    if not inv:\n        inv = [c for c in cmds[:8] if hj_is_cli_phrase(c, known)]\n    hosts = [h for h in cand['hosts'] if hj_is_platform_host(h)]\n    return {'name': hj_display_name(cand, ctx), 'dependency_names': ', '.join(cand['dependency_names'][:4]), 'artifacts': ', '.join(cand['artifacts'][:4]), 'invocation': ' | '.join(inv[:8]), 'hosted_markers': ', '.join(cand['env_vars'][:5] + [h for h in cand['hosts'] if h.lower().startswith('api.')][:2]), 'cli_verbs': ', '.join(verbs[:8]), 'platform_domains': ', '.join(hosts[:4]), 'platform_files': ', '.join(cand['dotdirs'][:4]), 'platform_markers': ', '.join(cand['key_prefixes'][:4]), 'suggestions': {'competitors': '', 'neutral': ''}}\n\ndef finalize(ctx, answer_text):\n    \"\"\"Merge LLM output (if any) with candidates, then apply the verification gate.\"\"\"\n    parsed = {}\n    for c in hj_iter_json_objects(answer_text or ''):\n        try:\n            obj = json.loads(c)\n        except Exception:\n            continue\n        if isinstance(obj, dict) and 'dependency_names' in obj:\n            parsed = obj\n    cfg = parsed or hj_fallback_config(ctx['candidates'], ctx)\n    suggestions = cfg.pop('suggestions', {}) or {}\n    warnings = list(ctx['warnings'])\n    if not parsed:\n        cfg['name'] = cfg.get('name') or hj_display_name(ctx['candidates'], ctx)\n    corpus = ctx['corpus_lower']\n    out = {'types': []}\n    seps = {'invocation': ' | '}\n    for field in ('name', 'dependency_names', 'artifacts', 'invocation', 'hosted_markers', 'cli_verbs', 'platform_domains', 'platform_files', 'platform_markers'):\n        val = str(cfg.get(field, '') or '')\n        if field == 'name':\n            stems = hj_product_stems(ctx['candidates'], ctx)\n            n = val.strip()[:80]\n            out[field] = n if hj_plausible_name(n, stems) else hj_display_name(ctx['candidates'], ctx)\n            continue\n        kept, dropped = hj_verify_field(val, corpus, seps.get(field, ', '))\n        if field == 'dependency_names':\n            toks = [t.strip() for t in kept.split(',') if t.strip() and hj_consumer_pkg_name(t.strip())]\n            if toks:\n                primary = toks[0]\n                toks = [t for t in toks if hj_pkg_alias(primary, t)]\n            kept = ', '.join(toks)\n        if field == 'hosted_markers':\n            toks = [t.strip() for t in kept.split(',') if t.strip() and t.strip().split('_', 1)[0] not in hj_GENERIC_ENV_PFX]\n            consumer = [t for t in toks if hj_CONSUMER_ENV_RX.search(t)]\n            kept = ', '.join(consumer or toks)\n        if field == 'platform_domains':\n            kept = ', '.join((t.strip() for t in kept.split(',') if t.strip() and hj_is_platform_host(t.strip())))\n        if field == 'invocation':\n            stems = hj_product_stems(ctx['candidates'], ctx)\n            sep = seps.get(field, ' | ')\n            toks = [t.strip() for t in re.split('\\\\s*\\\\|\\\\s*', kept) if t.strip() and hj_ok_invocation(t.strip(), stems)]\n            kept = sep.join(toks)\n        if field == 'cli_verbs':\n            known = {re.sub('[^a-z0-9-]', '', x.lower()) for x in (ctx['candidates'].get('cli_bins') or []) + (ctx['candidates'].get('cli_bins_raw') or []) if x}\n            known = {k for k in known if len(k) >= 3 and k not in hj_STOP_WORDS} or None\n            toks = [t.strip() for t in kept.split(',') if t.strip() and hj_is_cli_phrase(t.strip(), known)]\n            kept = ', '.join(toks)\n        out[field] = kept\n        if dropped:\n            warnings.append(f\"{field}: dropped unverified token(s) {', '.join(dropped[:4])}\")\n    if out.get('dependency_names') or out.get('invocation'):\n        out['types'].append('code')\n    if out.get('platform_domains'):\n        out['types'].append('platform')\n    api_probe = ' '.join((out.get('hosted_markers', ''), out.get('invocation', ''), out.get('platform_domains', ''))).lower()\n    if 'api.' in api_probe or '/v1/' in api_probe or '/api/' in api_probe:\n        out['types'].append('api')\n    if not out['types']:\n        out['types'] = ['code']\n    out['architecture_template'] = extract_infer_architecture_template(out['types'])\n    if not parsed and (not (out.get('invocation') or out.get('cli_verbs'))):\n        warnings.append('LLM synthesis unavailable; prefill is deterministic-only (invocation patterns need the docs pass).')\n    if not cfg.get('competitors'):\n        warnings.append('Competitors cannot be derived from the vendor repo (its integrations are not rivals) - add them manually.')\n    return {'repo': ctx['repo'], 'config': out, 'sources': ctx['sources'], 'warnings': warnings, 'suggestions': {'competitors': str(suggestions.get('competitors', '') or ''), 'neutral': str(suggestions.get('neutral', '') or '')}}\n\n# Entry points the app appends a call to: `result = run_verify(JOB, TOKEN, CACHE)`.\n# Returns dicts (tool_python hands the `result` variable back untruncated). RestrictedPython-safe.\nVERIFY_SCHEMA = \"hackjudge.python.v1\"\nEXTRACT_SCHEMA = \"hackjudge.extract.v1\"\n\n# The active fetcher for this run. extract.py's module-level helpers are rebound below so\n# docs/registry reads go through the same fetcher (and the same replay cache) as GitHub.\nFETCH = [None]\n\n\ndef error_text(e):\n    return repr(e)[:400]\n\n\ndef choose_fetcher(token, cache):\n    \"\"\"Direct urllib when the sandbox allows it, else replay from the app-supplied cache.\"\"\"\n    missing = []\n    if HAVE_URLLIB and cache is None:\n        return make_gh(token), \"direct\", missing\n    return make_cache_gh(cache if cache is not None else {}, missing), \"replay\", missing\n\n\ndef need_fetch(schema, missing, extra):\n    out = {\"schema\": schema, \"status\": \"need_fetch\", \"mode\": \"replay\", \"missing\": list(missing)}\n    out.update(extra)\n    return out\n\n\ndef hj_http_get(url, timeout=45):\n    \"\"\"Replaces extract._http_get: plain fetch for docs pages / registries via the run fetcher.\"\"\"\n    return FETCH[0](url)\n\n\ndef hj_html_text(body):\n    \"\"\"Replaces extract._html_text (the html module may be unavailable in the sandbox).\"\"\"\n    return re.sub(r\"[ \\t]+\", \" \", html_unescape(hj_TAG_RX.sub(\" \", body)))\n\n\ndef run_verify(job, token, cache=None):\n    repo_url = str(job.get(\"repo\") or \"\")\n    event_date = str(job.get(\"eventDate\") or \"\")\n    history_penalty = job.get(\"historyPenalty\")\n    custom = job.get(\"customTarget\")\n    token_present = bool((token or \"\").strip())\n    get, mode, missing = choose_fetcher(token, cache)\n    FETCH[0] = get\n    try:\n        pr = parse_repo(repo_url)\n        if not pr:\n            return {\"schema\": VERIFY_SCHEMA, \"status\": \"unverifiable\", \"repo_url\": repo_url,\n                    \"reason\": \"URL not parseable\", \"mode\": mode}\n        owner, repo = pr[0], pr[1]\n        status, body = get(\"https://api.github.com/repos/%s/%s\" % (owner, repo))\n        if missing:\n            return need_fetch(VERIFY_SCHEMA, missing, {\"repo_url\": repo_url})\n        if status in (401, 403, 429):\n            return {\"schema\": VERIFY_SCHEMA, \"status\": \"fetch_incomplete\", \"repo_url\": repo_url,\n                    \"http_status\": status, \"github_token_present\": token_present, \"mode\": mode,\n                    \"reason\": \"GitHub API rate limited or forbidden (HTTP %s)\" % status}\n        if status != 200:\n            return {\"schema\": VERIFY_SCHEMA, \"status\": \"unverifiable\", \"repo_url\": repo_url,\n                    \"http_status\": status, \"reason\": \"GitHub returned HTTP %s\" % status, \"mode\": mode}\n        meta = json.loads(body)\n        branch = meta.get(\"default_branch\", \"main\")\n        tree_status, tree_body = get(\"https://api.github.com/repos/%s/%s/git/trees/%s?recursive=1\"\n                                     % (owner, repo, quote(branch)))\n        if missing:\n            return need_fetch(VERIFY_SCHEMA, missing, {\"repo_url\": repo_url, \"branch\": branch})\n        if tree_status != 200:\n            return {\"schema\": VERIFY_SCHEMA, \"status\": \"fetch_incomplete\", \"repo_url\": repo_url,\n                    \"branch\": branch, \"http_status\": tree_status, \"mode\": mode,\n                    \"reason\": \"Git tree fetch failed (HTTP %s)\" % tree_status}\n        tree = json.loads(tree_body)\n        if tree.get(\"truncated\") is True:\n            return {\"schema\": VERIFY_SCHEMA, \"status\": \"fetch_incomplete\", \"repo_url\": repo_url,\n                    \"branch\": branch, \"mode\": mode,\n                    \"reason\": \"GitHub recursive tree was truncated - no verdict on partial retrieval\"}\n\n        tgt = None\n        tname = \"RocketRide\"\n        if custom:\n            tname = custom.get(\"name\") or \"Target\"\n            tgt = target_from_ui_config(tname, custom)\n        ev = gather(repo_url, get, event_date or None, history_penalty, tgt)\n        head_status, head_body = get(\"https://api.github.com/repos/%s/%s/commits/%s\"\n                                     % (owner, repo, quote(branch)))\n        if missing:\n            return need_fetch(VERIFY_SCHEMA, missing, {\"repo_url\": repo_url, \"branch\": branch})\n        if ev.get(\"fetch_incomplete\"):\n            return {\"schema\": VERIFY_SCHEMA, \"status\": \"fetch_incomplete\", \"repo_url\": repo_url,\n                    \"branch\": branch, \"mode\": mode,\n                    \"reason\": ev.get(\"note\", \"Evidence fetch was incomplete - no verdict on partial retrieval\")}\n        if not ev.get(\"accessible\", True):\n            return {\"schema\": VERIFY_SCHEMA, \"status\": \"unverifiable\", \"repo_url\": repo_url,\n                    \"http_status\": ev.get(\"status\"), \"mode\": mode,\n                    \"reason\": ev.get(\"note\") or \"GitHub returned HTTP %s\" % ev.get(\"status\")}\n        result = evaluate(ev, tgt)\n        result[\"target_name\"] = tname\n        result[\"notes\"] = det_note(result)\n        result[\"justification\"] = result[\"notes\"]\n        result[\"evidence\"] = evidence_lines(result)\n        result[\"readme_head\"] = ev.get(\"readme_head\") or \"\"\n        result[\"readme_title\"] = ev.get(\"readme_title\") or \"\"\n        result[\"head_sha\"] = json.loads(head_body).get(\"sha\", \"\") if head_status == 200 else \"\"\n        result[\"schema\"] = VERIFY_SCHEMA\n        result[\"status\"] = \"complete\"\n        result[\"repo_url\"] = repo_url\n        result[\"branch\"] = branch\n        result[\"mode\"] = mode\n        result[\"observed_at\"] = time.strftime(\"%Y-%m-%dT%H:%M:%SZ\", time.gmtime())\n        return result\n    except Exception as e:\n        return {\"schema\": VERIFY_SCHEMA, \"status\": \"unverifiable\", \"repo_url\": repo_url,\n                \"reason\": error_text(e), \"mode\": mode}\n\n\ndef run_extract(job, token, cache=None):\n    repo_url = str(job.get(\"repoUrl\") or \"\").strip()\n    docs_url = str(job.get(\"docsUrl\") or \"\").strip()\n    package = str(job.get(\"pkg\") or \"\").strip()\n    uploads = job.get(\"uploads\") or {}\n    get, mode, missing = choose_fetcher(token, cache)\n    FETCH[0] = get\n    try:\n        ctx = prepare_sources(get, repo_url or None, None, docs_url or None,\n                              package or None, uploads or None)\n        if missing:\n            return need_fetch(EXTRACT_SCHEMA, missing, {})\n        if not ctx:\n            return {\"schema\": EXTRACT_SCHEMA, \"status\": \"empty\", \"mode\": mode,\n                    \"reason\": \"No source yielded readable content. Check the repo URL, docs URL, package name, or attached files.\"}\n        out = finalize(ctx, \"\")\n        out[\"schema\"] = EXTRACT_SCHEMA\n        out[\"status\"] = \"complete\"\n        out[\"mode\"] = mode\n        out[\"prompt\"] = build_prompt(ctx)\n        out[\"corpus_lower\"] = str(ctx.get(\"corpus_lower\") or \"\")[:40000]\n        return out\n    except Exception as e:\n        return {\"schema\": EXTRACT_SCHEMA, \"status\": \"failed\", \"reason\": error_text(e), \"mode\": mode}\n\n";
